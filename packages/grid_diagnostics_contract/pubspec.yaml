name: grid_diagnostics_contract
version: 0.2.1
description: Grid-local station-lock record and bearer subprotocol contract.
repository: https://github.com/memento-engineering/the_grid
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies: {}

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
