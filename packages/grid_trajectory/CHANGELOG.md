# Changelog

## 0.1.0

- Stage 0 substrate (tg-zfek): §1 envelope + sealed §2 record codec with
  checked-in golden fixtures, §4 DDL bootstrap, the §5 fenced append client
  (epoch claim, T6i counter-CAS, belt predicates, dolt-commit cadence), the
  service-tick skeleton, and the `traj show` / `traj shadow-diff` verbs.
