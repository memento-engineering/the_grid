name: grid_exploration
description: >-
  Exploration-protocol host for the_grid — GridControllerPlugin speaking
  lenny's ext.exploration.* wire protocol over the Dart VM service.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  beads_dart: any
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
  vm_service: ^14.2.0
