name: grid_cockpit_ui
description: Transport-neutral Flutter projections and primitives for grid diagnostics.
publish_to: none
repository: https://github.com/memento-engineering/the_grid
resolution: workspace

environment:
  flutter: ">=3.41.0"
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.0.0
  grid_diagnostics_contract:
    path: ../grid_diagnostics_contract
  state_notifier: ^1.0.0

dev_dependencies:
  build_runner: ^2.4.0
  flutter_test:
    sdk: flutter
  freezed: ^3.0.0
  lints: ^5.1.0
