floor: v1.0.5
# The newest bd RELEASE this package has been exercised through. The publish
# guard refuses a release while upstream's latest tag is older than this, so
# the value must name a tag that EXISTS: pointing it at an unreleased version
# embargoes every beads_dart release, including ones that have nothing to do
# with that bd version (it blocked 0.2.0-rc.6 on 2026-09-02).
#
# bd 1.3.0 is rehearsed — the rc fixtures and drift pins are in
# fixtures/upstream/2026-09-02-bd-1.3.0-rc.1/ — but 1.3.0 itself is not
# released. Move this to v1.3.0 when it is; the gate stays open at that point
# because upstream's latest will equal it.
day_one_wait_through: v1.2.2
fork_patches:
  graph_apply_parent_cycle_skip:
    repository: nicholasspencer/beads
    base_sha: f9fe4ef2a6d3d90b52f1b62df15a5b2c0833c82b
    ref: grid-v1.0.5-graph-apply-parent-cycle-skip.1
    fixture_set: 2026-09-01-bd-grid-v1.0.5-graph-apply-parent-cycle-skip.1
    resolved_sha_receipt: fixtures/upstream/2026-09-01-bd-grid-v1.0.5-graph-apply-parent-cycle-skip.1/README.md
