# the_grid

A Dart-native reactive orchestrator that will replace Gas City (`gc`). The long bet:
**apps are built in Flutter, Lenny debugs apps, Lenny files bugs into the grid — the grid
builds everything.**

## Read first, in order

1. `docs/PDR.md` — vision, goals, milestones, acceptance criteria, constraints, **the gate (§9)**
2. `docs/adr/ADR-0000-ai-decision-register.md` — **process-critical, see below**
3. `docs/adr/ADR-0001…0004` — ratified technical decisions (foundations, packages/projections, reconciler, runtime/tmux)
4. `docs/M1-BUILD-ORDER.md` — M1 dependency-ordered work breakdown (**M1 done — see below**)
5. `docs/M2-BUILD-ORDER.md` — M2 (reconciler) orchestration spine, maps ADR-0003 → tracks (**M2 done, merged**)
6. `docs/M3-BUILD-ORDER.md` — M3 (runtime) dependency-ordered tracks + the Friday dogfood loop (**discovery drafted 2026-06-15, pending ratification**)
7. `docs/adr/ADR-0006-dogfood-rig-and-live-write-authorization.md` — **Proposed**: M3 live-write authorization + bead-shaped ownership gate + land policy
8. `docs/M4-SCOPING.md` — usage-driven M4 decomposition + fs adoption ladder (proposal, ADR-0000 A6)
9. The `predictable-flutter` skill (installed at `.claude/skills/predictable-flutter/`) — the architecture all code follows

**M1 shipped (2026-06-11):** the reactive kernel is built, tested (183 offline + 5 integration green), and committed on branch `m1-reactive-kernel` (packages `grid_controller`/`grid_exploration`/`grid_cli`/`grid_devtools`). `grid demo` proves it. ADR-0000 A8–A14 record the en-route AI decisions (pending Nico).

**M2 built (2026-06-13) — pending ratification:** the convergence reconciler is built, tested (771 offline + 49 integration green), and committed on branch `m2-reconciler` (new pkg `grid_reconciler`). All spine tracks done — `0→A→{B,D,F}→{C,E}→G→H` (domain+codec, reducer, gates, ready-work SQL, recovery, actuator, runtime+shadow, conformance). DoD `docs/M2-BUILD-ORDER.md` §"Definition of done": 1✅2✅3✅4◐(codec fidelity + shadow *mechanism* now validated offline vs REAL gc bytes — A29; live-traffic diff half = M3 dogfood)5✅6✅. **ADR-0000 A15–A29 RATIFIED (Nico, 2026-06-14)** and promoted into their home docs — A15/A16 → ADR-0003 D4, A19 → D2, A24 → D5, A17 → ADR-0002 D2, A22/A23/A25/A26/A27 → ADR-0003 **Decision 8**, A18 → 31-key schema, A20/A21 → ADR-0001 D4/D5, A28/A29 → DoD criteria 1/4. **A30/A31 record the genesis relationship** (re-evaluation of all ADRs vs `engineering.memento/genesis`): adopt nothing into the engine — the_grid is a genesis consumer only at the surface/render layer, deferred to a future **ADR-0005** + TUI-inspector spike (**M3**). Track I codec-fidelity half **done** (A29; pinned `fixtures/upstream/2026-06-11-bd-1.0.5/convergence/`). **M2 merged to `main` (2026-06-15)**; `m2-reconciler` pushed; now on branch `m3-runtime`. Carried to M3: a **recovery actuator** (A27, Track 4b), the **Track I live-shadow** run, and **genesis adoption** (A31, future ADR-0005). **Side-finding for Nico:** a plaintext `CLAUDE_CODE_OAUTH_TOKEN` is visible on the live gascity control-dispatcher's process argv.

**M3 discovery drafted + ADR-0006 RATIFIED (Nico, 2026-06-15):** `docs/M3-BUILD-ORDER.md` (runtime tracks + the Friday dogfood loop) and **ADR-0006** (dogfood rig + live-write authorization + ownership gate + land policy) are written and **ADR-0006 is Accepted**. **A32** (bead-shaped ownership predicate + shared rig allow-set + single bd write chokepoint — M2's `OwnsRigs(Convergence)` is structurally uncallable on a ready work `Bead`) and **A33** (`plugins`→`extensions` exploration wire-key rename so a stock leonard attaches to the_grid's Dart VM) are **ratified**; **A34** (tmux → **`genesis_tmux`**, a zero-dep genesis package consumed as a path dep; handoff at `genesis/packages/tmux/HANDOFF.md`) and **A35** (ratified dogfood inputs) recorded. **Ratified inputs:** rig `tgdog`; root checkout `engineering.memento/lenny-tgdog` (explicit registration); agent token via operator env + allowlist (`includeParentEnvironment:false`, never argv); pre-granted agent perms; leonard `--extensions grid`. **The dogfood goal: by Fri 2026-06-19, the_grid BUILDS lenny (spawns Claude Code agents per ready bead) and leonard DEBUGS the_grid's pure-Dart VM directly via `ext.exploration.*`.** **M3 build DELIVERED to the live-arm threshold (2026-06-15)** — Tracks 2–7 built across the new `grid_runtime` pkg (+ `grid_cli` `grid run`); **951 offline tests green, 0 failures, `melos analyze` clean** across all 6 packages; nothing live touched (fakes + temp repos + `sh` stubs + `grid run --dry-run`). Uncommitted on branch `m3-runtime` (27 paths). Off the Friday path: Track 1 tmux = **`genesis_tmux`** (handoff staged, build not started), Track 4b recovery actuator (deferred). **The one human gate held for Nico: the first LIVE arm** — creating the `lenny-tgdog` clone + the first real bd writes beside the running gc + spawning agents that open lenny PRs — **and blessing the 2 specific lenny work beads** (deferred to that gate).

**M3 LIVE ARM PROVEN (2026-06-17) — the_grid BUILT genesis, autonomously.** Nico re-targeted arm #1 from lenny to the **genesis** backlog (A36): thinner, owned (embedded `genesis` DB, no gc nearby), and the two real beads bootstrap the_grid's own M4 substrate. The live arm ran and **the_grid spawned coding agents per ready bead that built both features** — `feat(tree): MultiChildSeed/MultiChildBranch` (genesis-7r9) + `feat(tree)!: first-class Key type` (genesis-q8h) — committed local-first into per-bead worktrees off a `genesis-grid` clone, **genesis never written to**. Session/lifecycle beads go to a **separate the_grid-owned `tgdog` state DB** (A37 = the resolved "session beads where?" sub-decision; genesis stays a pristine work source — it can't even adopt the_grid's `session` type). `grid run` grew `--workspace` (read a side-car'd repo), `--bead` (the blessed drive-list), `--state-workspace`/`--state-rig` (split read/write). **Fixes en route (A38, incl. an adversarial-review pass):** the `oneTurn` exit fix (detached spawn → no exit code → a *successful* one-shot agent was quarantined; now a `oneTurn` vanish → `Exited(0)`/clean close, confirmed live: session `tgdog-2xk` closed `crash_count=0`); a fail-closed guard so a live run refuses to default sessions into the read workspace; `_dispatch` conservative unwind (reap orphan worktree + close orphan session on a post-provision failure); per-bead failures reported, never crash the controller. **951→ M3 offline tests + these (147 in the two touched pkgs) green, `melos analyze` clean.** Agent auth: spawned `claude` authenticates via the macOS keychain (no token env needed — proven by probe). **Carried (A38, not blocking):** leonard-debugs-the_grid is wired (`ext.exploration.*` host + a live VM URI each run) but **unexercised**; non-Anthropic backend (swift-infer) needs a coding-harness/provider shim (parked by Nico); orphan-recovery across a controller restart (`listBeadWorktrees` sweep) has no caller; the `land()`→PR step is a deliberate human follow-up. **lenny remains arm #2** (where tg-coexistence + the "grid builds lenny" headline are taken on).

**M3 DEBUG HALF EXERCISED (2026-06-18, tg-e28, A40) — the mutual loop is closed.** The "leonard DEBUGS the_grid" half (wired-but-**unexercised** at A38) is now **proven live**: a stock, **credential-free `leonard_drive`** (lenny's zero-model VM driver — *not* `leonard_cli`, which demands an inference key before it attaches and so self-skips unarmed) attached to a real `grid run --dry-run` and read the live controller over `ext.exploration.*` — `tools` handshake (the `grid` namespace + 5 tools), `observe` (live grid state under `extensions.grid.data`, `readPath:"cli"`, stats ticking between calls), `invoke grid.{ready,snapshot,stats}`. **Reactivity proof:** closing a bead in the workspace surfaced `beadClosed`·`beadUpdated`·`readySetChanged` over the wire — leonard watched the_grid's kernel *react* in real time. Exercised the **existing** host (already pull-free per A39: out-of-band event stream + synchronous pull reads); the literal genesis `StatefulPerception` rebuild stays deferred to ADR-0005. New non-skipping `grid_exploration/test/leonard_drive_attach_test.dart`; coexistence-safe (self-owned temp `dbg-` repo, dry-run default, `tg`/live-gc untouched); no lib changes. **Still carried:** swift-infer coding-harness shim (parked), `listBeadWorktrees` restart-rebind (no caller), `land()`→PR (human follow-up); **lenny arm #2** open.

**M4 PIVOT — "the running grid IS a tree" (2026-06-24, design decided; ADR-0007 is the gating ratification).** A long design session + a working `genesis_tree` spike + a 5-lens adversarial review concluded the M2/M3 reducer/actuator was a *hand-rolled reimplementation of reconcile*; **M4 makes `genesis_tree` the engine** — `build(observed)` → the running system; keyed reconcile + Branch lifecycle = the work lifecycle (mount=spawn, unmount=kill, phase=a reconcile transition). **Riverpod DROPPED → plain `StateNotifier`** (un-diverges from predictable-flutter/lenny; reverses this file's "Riverpod 3 / never StateNotifier" rule below); StateNotifiers provided via `InheritedSeed`, observed by `StatefulSeed`s; flush isolation is **observational** (only the node observing the changing notifier marks dirty — **never `root.markNeedsRebuild()`**); config nodes are **ancestors** of work nodes. Crash-safety P0 = **respawn-or-skip** (durable completion + pgid/token + freshness barrier; *adopt-a-live-process deferred*). Capabilities = effect-Seeds (carrier `EffectSeed` keyed by capId for dynamic/TOML). **Reverses A30 (all 3 clauses) + A31's "does NOT mount beads as a domain tree" + the Riverpod convention — via ADR-0007 (explicit quote-and-supersede, never silent).** Design: **`docs/M4-PDR.md`**. **ADR-0007 RATIFIED (Nico, 2026-06-24; Accepted) — ADR-0005 retired; §6 stamps applied to A30/A31/A39/A6 + ADR-0001 D3/ADR-0002 D2/CLAUDE.md/M4-SCOPING. `docs/M4-P0-BUILD-ORDER.md` WRITTEN + HARDENED (v2, 30-finding 5-lens pass). WorkPhase corrected (A40, Nico 2026-06-24): cursor on the_grid's OWN session bead + `phaseOf` by JOIN (work-bead presence + session cursor) — the v1 "grid.phase on the work bead" shape was incompatible with the A37 split-store (foreign work source is read-only); positive-terminal-only unmount (never ready-set-exit); `verify` not `gate`; `type=convergence` excluded at the mount boundary. NEXT = BUILD Wave 1 (Track A — the pure Seeds, zero I/O, writes the derailment-invariant-1 guardrail), then B∥C → D∥E/F → G.** Lesson: scrutinize autonomous ADRs / spike before doctrine / lean to Nico's own stack.

**M4 P0 BUILT (2026-06-25) — the tree engine is real, offline-green, on branch `m4-p0-tree-engine`.** All waves landed as a hybrid: central authorship of the load-bearing cores (Track A Seeds + the kernel + the shared session-bead contract) + ultracode workflows (implement → adversarial/mutation verify → fix) for the mechanical waves. **New pkg `grid_engine`** (76 tests): the `Grid → RigScope → Rig → WorkList → WorkBead → effect Seed` tree on `genesis_tree` (cross-workspace path-dep, resolution de-risked); the `GridJoinBridge` (the lone pipeline subscription, A39); the `EffectSeed` carrier (mount=spawn, SessionStarted=persist pgid/token, completion=advance the cursor, dispose=kill, with the `_cancelled`/`mounted`/`_completed` guards); the opinion-light `GridKernel` (microtask flush loop, `root.markNeedsRebuild` never called) + the compiled `DefaultExtension` (Agent/Verify/Land effects + resolver, opinions fenced to `lib/src/extension/` by a structural test); the `RestartReconciler` (respawn-or-skip; SKIP fires for a FOREIGN unwritable work bead because the cursor is on the_grid's own session bead — the v1 bug fixed; the REAL `terminateGroup` guard). **The four derailment-invariants are enforced as mutation-verified tests** (no-pipeline-subscription, only-the-chokepoint-writes, convergence-never-mounts, A37-pristine-source) + the PDR §7 acceptance suite. `grid_cli` wired offline (`composeRunTree` + the `RuntimeSnapshotSource` adapter + a dry-run smoke + the `grid phase --advance` working-agreement line). **`melos analyze` clean + the full offline suite green across all packages.** **A41 RATIFIED (Nico, 2026-06-25)** (the fail-closed `IssueType.isCore` allow-list at the mount boundary — refines A40's deny-list after a hardening pass proved `bd ready` leaks the_grid's orchestration customs; `isCore` stands, epic/milestone/decision retained). **ADR-0002 amended** (the `grid_engine` row, per the ratified A40). **The first LIVE arm is held as the human gate** (no live `grid run --tree`, no real `claude`/`git`/`tg` touched, nothing pushed — per the safety rails / M3 precedent). **Deferred (explicit):** adopt-a-live-process; TOML `PackInflater`/full `GridExtension` (P1/ADR-0008); the full Riverpod purge of `grid_controller`'s 9 `AsyncValue` files (M4 DoD); the `grid phase --advance` shim binary (live arm).

**vNext DESIGN RATIFIED — ADR-0008 ACCEPTED (Nico, 2026-06-27).** A federation + authoring-SDK design session graduated to **`docs/adr/ADR-0008-authoring-sdk-and-reentrant-engine.md` (Accepted)** + the design surface **`docs/SCRATCH-vnext-prd.md`**. It ratifies: the **"full power grid" rename** — **`Grid`→`Station`** (the machine), **`Rig`→`Substation`** (the project), **`Grid` repurposed** = the federation (per-station, **no center**, git-style; a "GridHub" is an optional downstream product), **`Asset`** = `*_grid_assets` packs (the gc-pack-protocol home: `station_grid_assets` baseline/dogfood [replaces `the_rig`], `butane_grid_assets`, `zero_conf_grid_assets`); the public SDK package **`the_grid`** over the now-**private `grid_engine`** (consumers compose, never subclass); the **Formula** (declared step-graph) / **Capability** (opaque leaf) / **Service** (pluggable collaborator) / **Asset** vocab; the **REENTRANT engine** (a formula's step-graph inflates into a reconciled subtree); **state-restoration crash-recovery** (Flutter-modeled — `RestorableProperty`/bucket→dolt; **adopt-a-surviving-detached-process is the floor**; prototype the_grid-local against the `Burn`, then **extract to genesis** post-prototype); **supervision + the restorable circuit-breaker** (per-(station×work); exhaustion escalates to a human); **resource governance** (`DartEnvironment`/governor; declare-and-check bounded now, dynamic planning later); and the **pluggable-abstract-domains meta-pattern** (the engine knows `SourceControl`/`Trust`/transport in *concept*, not *detail* — impls ship in assets; **`Trust`** = local/reputation/ledger, "less-blockchain-by-default, ledger-as-plugin", distinct from **`consent`**=`genesis_consent`=authorization). **RENAME APPLIED IN CODE (2026-06-27).** The Station/Substation migration landed whole-repo — `melos analyze` clean + full offline suite green (engine 76 / controller 200 / reconciler 587 / runtime 130 / cli 21 / exploration 13 / devtools 15), counts unchanged from baseline. **Nico's scoping (this session):** whole-repo; engine machinery → `Station*` (`GridKernel`→`StationKernel`, `GridJoinBridge`→`StationJoinBridge`, `GridBeadWriter`→`StationBeadWriter`, `GridGitService`→`StationGitService`, file renames + freezed regen); the_grid's lowercase ownership vocab flipped to `substation` (`OwnsRigs`→`OwnsSubstations`, `ownedRigs`/`stateRig`/`workRig`/`sessionRig`/`rigId`/`rigDir`…) **+ CLI flags `--rig`/`--state-rig` → `--substation`/`--state-substation`** (no back-compat alias — no shipped users). **The codec boundary held:** the persisted gc key stays `metadata.rig` (`StationBeadWriter.rigKey = 'rig'`), and gc's convergence byte-port schema (`ConvergenceFields.rig`/`convergence.rig`/`FieldRig`/`eventRig`/`withEventRig`), `IssueType.rig`, and `kGridNamespace = 'grid'` (leonard's `--extensions grid`, A33) are all **unchanged** — only the_grid's own types/vocab moved. Product `Grid*` names (`GridControllerRuntime`/`GridExplorationHost`/…) and package names (`grid_engine`/`grid_runtime`/…) **unchanged** (the product is still "the grid"; the `the_grid` SDK package is a later task). Forward-pointer stamps applied on ratification to **ADR-0007 D1** + **ADR-0002 Decision 1** (one-line amendments) — never silent. **ADR NUMBERS ASSIGNED (Nico, 2026-06-27): append** — **Federation = ADR-0011**, **Observability = ADR-0012** (ADR-0007 §5's reserved P2/0009 "unified topology tree" + P3/0010 "convergence-as-subtree" stay reserved as-is; the two new ADRs append after them). Both still UNWRITTEN/parked: a **Federation ADR (0011)** (per-station `Grid` views, dolt-truth + lossy inter-station bus, capability-durable/capacity-ephemeral, claim-in-own-store + deterministic resolution, presence/heartbeat + disconnect-gossip, reaping, the pub/sub event flow, `Trust` impls) + an **Observability ADR (0012)** (observable-source first-class, OTel ⊥ perception as co-emitted sinks, the AOT HTTP/WS exploration transport under perception+consent). **The first LIVE arm remains the human gate.**

**M4-P1 BUILT (2026-06-27) — the REENTRANT engine is real, offline-green, on branch `m4-p1-reentrant-engine`.** Built per `docs/M4-P1-REENTRANT-ENGINE-BUILD-ORDER.md` (the D-1…D-7 decisions promoted to **ADR-0007**/**ADR-0008** amendments earlier this session) as a hybrid: central authorship of the load-bearing cores + per-wave **read-only-`Explore` adversarial review (refute-verified)** → fold → commit. The engine drops in BELOW `WorkBead` at the existing `EffectResolver` seam: `Station → Substation → WorkList → WorkBead → SessionScope → FormulaScope → _FormulaChildren → CapabilityHost → Capability`. **The 10 tracks** (`grid_engine` **164 offline tests**, +D-1 in `grid_runtime`; full workspace 200/587/134/13/21/15 green, `melos analyze` clean): **A** the `Formula`/`FormulaStep`/cursor value-types + the pure frontier predicate (the SDK surface behind `lib/src/sdk/`); **B** the per-node `grid.cursor.{path}.*` codec (D-3, additive — coexists with the legacy `WorkPhase` cursor); **D-1** the `StationBeadWriter` per-target-id serialization queue (the same-key write race); **C** `SessionScope` (D-2: adopt-or-mint ONCE above the fan-out, provides a stable `InheritedSeed<SessionHandle>`, owns the positive-terminal close + the breaker-exhaustion escalation); **D** the pure `FormulaScope` inflater + `_FormulaChildren` keyed container + the `CapabilityRegistry`/`FormulaResolver` seam; **F** per-node respawn-or-skip (D-4); **E** the `CapabilityHost` carrier (EffectSeed generalized off `WorkPhase`) + the opaque `Capability`/`ServiceCapability`/sandboxed `CapabilityContext`/`ServiceBundle`/`SourceControl` SDK; **G** supervision + the restorable circuit-breaker (D-5: the failing leaf authors the restart cursor, `SessionScope` escalates+tears-down, the kernel owns the backoff Timer + re-poke — never `root.markNeedsRebuild`); **H** agent/verify/land as `Capability` impls + the `code` formula (no behavior change, additive); **I** the four derailment-invariants as mutation-resistant gates **AT DEPTH**; **J** the **Burn** end-to-end offline (fan-out + await-all barrier + daemon + close, AND the failure path: escalate + teardown of the leaked daemon). Commits: `b5a82fd`(A) `304ca6d`(B) `5e6c99e`(D-1) `f2f557c`(C+D) `49ac7cf`(F) `1a0f4fe`(E) `656aae8`(G) `7c94e39`(H) `d1aa69e`(I+J) `6c98b39`(review-fold). **Process lesson (recorded):** a general-purpose review agent (Edit-capable) injected stray mutations into the working tree during the FIRST Track A review (`isRetired`→false, caught; `restartCount<` →`<=` with a `// MUT F3` marker, MISSED and committed in Track A, masked by `isCircuitBroken`, then caught + reverted by the final review). **All later reviews use the read-only `Explore` agentType** so this cannot recur. **DEFERRED (explicit — behind the human gate / a cleanup pass):** the live `composeRunTree` swap onto `FormulaResolver` (+ providing `CapabilityRegistry`/`ServiceBundle` in the kernel mountRoot); the `WorkPhase`/`EffectSeed`/old-`DefaultExtension` deletion (dead after the swap); `restForOne` transitive-dependent re-keying (P1 = barrier-equivalent to `oneForOne`); restoration/adopt-a-live-process (D-6, post-prototype); `FanOutStep` (dynamic planning); the `grid step --advance` CLI rename; the package split (`the_grid` SDK over private `grid_engine`, ADR-0008 D1). **The first LIVE arm remains the human gate** (no live `grid run --tree`, no real `claude`/`git`/`tg`, nothing pushed).

**M4-P1 FINISH-LINE CLEANUP LANDED (2026-06-28) — the reentrant engine IS the live path; the P0 `WorkPhase` path is deleted.** Executed the deferred-execution cleanup (offline, no new design): (a) `composeRunTree` swapped `DefaultEffectResolver` → `FormulaResolver(code)` + `buildCodeRegistry()` + a git `ServiceBundle`; `StationKernel.mountRoot` now provides the ambient `CapabilityRegistry` + `ServiceBundle` (stable, D-6 `updateShouldNotify=>false`). (b) DELETED the dead P0 path — `work_phase.dart`(`WorkPhase`/`phaseOf`), `effect_seed.dart`(`EffectSeed`), `default_extension.dart`(`Agent/Verify/LandEffectSeed`+`DefaultEffectResolver`); dropped the `grid.phase` codec (`parseWorkPhase`/`phaseCursorMetadata`/`SessionProjection.phase`) — **KEPT** the scalar `pgid/pid/token` restart fence (read by `RestartReconciler`, separate from the cursor). `EffectContext`/`EffectResolver` seams STAY (`FormulaResolver` is the sole `EffectResolver` impl; `effectFor({bead, session})`, no phase). (c) `grid phase --advance` → `grid step --advance` (the agent working-agreement prompt line — still the only "shim" reference; the binary stays deferred). (d) Migrated the P0 acceptance suite (the 4 derailment-invariant tests + PDR §7) + `station_kernel`/`join_bridge`/`session_bead`/`track_a` off `WorkPhase` onto the per-node cursor + `<sessionId>/<nodePath>` step-event names; deleted the 3 legacy test files (`effect_seed_test`/`land_effect_test`/`work_phase_test`); added shared `sessionBead`/`kCodeResolver` test helpers. **grid_engine 141** offline tests (164 − the 3 deleted P0 files), full workspace **200/587/134/21/13/15** green, `melos analyze` clean. **Two read-only-`Explore` adversarial reviews** (vacuousness + stray-marker/weakening) returned clean — every derailment invariant still enforced non-vacuously with a live sanity control. STILL DEFERRED: `restForOne` transitive re-keying; restoration/adopt-a-live-process; `FanOutStep`; the `the_grid` SDK package split (ADR-0008 D1); the `grid step --advance` shim binary. **The first LIVE arm remains the human gate** (nothing live touched, nothing pushed).

**ADR-0009 "THE ALLOCATION TREE" ACCEPTED (Nico, 2026-07-01) — the_grid's *third tree* on `genesis_tree`.** `genesis_tree` deliberately stops at `Seed`→`Branch` to *enable* consumer "third trees" (like `genesis_typesetting` / perception); the_grid's is the **Allocation Tree** — nodes = **`Allocation`s** (persistent, addressable managed objects holding a live effect: process / lease / tmux / app — the RenderObject-analogue), created by leaves AND scopes. The smells that motivated it (the `Expando`-keyed hold, `_capCtx ??=`, `CancelToken` juggling) were that tree jammed into the `Branch` layer. **Decisions:** relationships **LIGHT** (`Branch` + `InheritedSeed` + `SiblingView`; a Heavy linked tree is *enabled, not built* — earn it from a real relationship) **+ addressable, re-adoptable identity**; the **"sandbox" DISSOLVES** (invariants hold by *layering + a single write-locus* — the writer lives only in the Host, off-build — not a wall; **depending on context is the norm**, inv 1 = no *pipeline* sub, not no tree-dep); **lifecycle = four distinct verbs `startOrAdopt` / `update` / `dispose` (kill) / `detach` (leave)** (dispose NOT overloaded; `startOrAdopt` reattaches a **detached** survivor OR a **crash-orphan** on one path); **update-vs-replace + adoptability + detach are per-type** (`claude` replaces, tmux updates; daemon adopts+detaches, one-shot respawn-or-skip); `JobAllocation` convenience for one-shots. **Fills ADR-0007 §5's reserved P2/0009 "unified topology tree"** (Allocations ARE the addressable topology nodes) — stamps applied to **ADR-0007 §5 + ADR-0008 out-of-scope §** (never silent); **extends ADR-0007 + ADR-0008 D6** (restoration → `startOrAdopt`/`detach`); **feeds ADR-0012** (observability sinks). Design surface **`docs/SCRATCH-allocation-tree.md`**; impl plan **`docs/ADR-0009-ALLOCATION-TREE-BUILD-ORDER.md`** (offline tracks A–G; **the first LIVE arm — real adopt/detach across a controller restart — is the human gate**). **M6 federation is PARKED on branch `m6-federation` behind this** (grid_federation + ADR-0011 + the M6 build; the burn-follower becomes the first daemon-`Allocation` client and the M4-P1 daemon-reap bug dissolves).

## Process rules (non-negotiable)

- **The gate: OPEN as of 2026-06-11** (ADR-0001–0004 Accepted, ADR-0000 A1–A7 promoted, PDR §9 empty). The rule persists for new scope: doc before code; Nico ratifies explicitly.
- **ADR-0000 rule:** any decision made by AI goes into ADR-0000 as an amendment and stays there until Nico promotes it or shoots it down. Never write AI decisions directly into ADR-0001+ or silently change ratified docs to match your conclusions.
- Brainstorm → PDR → ADR → zero open questions → code. New scope gets a doc before it gets code.
- Fixtures are version-pinned (`fixtures/upstream/<date>-bd-<version>/`); re-capture via the porting skill's procedure, never edit by hand.

## Conventions

- Dart `^3.11.0`, pub workspace + melos (scripts: `bootstrap`, `test`, `analyze`, `format`), lints cloned from lenny (strict-casts/inference/raw-types, `prefer_single_quotes`, `unawaited_futures`, …).
- **freezed** sealed unions + `json_serializable` codecs; consume with exhaustive `switch` expressions. `build_runner` via melos.
- **`StateNotifier` + freezed** (the M4 tree-engine stack — ADR-0007 §6.6 Accepted 2026-06-24, reverses the former "Riverpod 3 / never StateNotifier" rule): StateNotifiers provided via `InheritedSeed`, observed by `StatefulSeed`s; `build()` stays pure (observe out-of-band, A39). *(Riverpod is removed with the M4 pivot; pre-M4 packages may still carry it until migrated.)*
- predictable-flutter layering: Services (stateless I/O) → Repositories (own one source, emit state) → Interactors/Selectors/Transformers → View. Reference types carry classifiers (`BdCliService`); value types are plain (`Bead`).
- Tests use **Fakes, not mocks**; pure logic (diff, projections, transitions) is tested before any IO is wired.
- APIs: **Futures for acts, Streams for observations.**

## bd / beads rules

- Always `BD_JSON_ENVELOPE=1`; assert `schema_version == 1`. Errors arrive **enveloped on stdout** with exit ≠ 0 (ADR-0001 Decision 4).
- Mutations: bd CLI only, `--actor grid-controller`. **Never SQL writes. Never touch `.beads/hooks/` (gc owns them).**
- Grouped mutations: `bd batch` (one transaction, one DOLT_COMMIT). Bulk reads: `bd export --include-infra` / `bd query` / multi-id `bd show`. **Never spawn bd per issue in a loop.**
- **Never call `bd show` from a re-query/controller path** — it writes `.beads/last-touched` and self-triggers the watcher.
- `bd list` does not surface infra-typed beads (agent/rig/role) — use export for those (ADR-0001 Decision 4).
- **Coexistence safety (ADR-0003 Decision 6):** gc's convergence handler assumes a single writer per bead. Never reconcile or mutate beads gc's reconciler owns; any shadow/conformance experiment against live convergence traffic is strictly read-only.

## Environment facts

- Dolt **server mode**: db `tg` at `127.0.0.1:34947`; local `.beads/dolt/` is empty. Creds `GC_DOLT_USER`/`GC_DOLT_PASSWORD`; `GT_ROOT=/Users/nico/gascity` (from `.beads/.env`). Server reaps idle connections at **30s** — pools must reconnect; keep ≤2 pooled connections.
- Cross-workspace writes into `tg` are routine (`.beads/routes.jsonl`) — `SELECT @@tg_working` (~1ms) is the authoritative change probe.
- gc keeps running during M1–M3; the_grid coexists (reads + bd-mediated writes only).
- Pinned upstream: **bd 1.0.5 (f9fe4ef2a)**. Sources on disk: gascity `~/development/com.gastownhall/gascity`, beads `~/development/com.gastownhall/beads`, lenny `~/development/com.nicospencer/lenny`, predictable-flutter `~/development/com.nicospencer/predictable-flutter`, **genesis** `~/development/engineering.memento/genesis` (the shared Seed/Branch/keyed-reconcile substrate; the_grid is a *future surface-layer consumer* only — **ADR-0000 A30**: the engine stays snapshot-diff + Riverpod + the gc-fidelity codec, genesis adopted only at the render edge in M3 per A31).
- Lenny M0 prerequisite (`ext.exploration.*` rename → `exploration_contract` extraction) is tracked as lenny beads `lenny-wisp-41rdl` → `lenny-wisp-9h557`; the_grid consumes `exploration_contract` as a **path dependency** during development. Only `grid_exploration` is blocked on it.

## Packages (ADR-0002)

`grid_controller` (sdk) · `grid_cli` (mgmt) · `grid_exploration` (lenny plugin) · `grid_devtools` (DevTools, only Flutter pkg, exploration-protocol-only) · `grid_reconciler` (M2) · `grid_runtime` (M3) · `tmux` (standalone general-purpose client, zero grid deps).
