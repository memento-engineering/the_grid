# Trajectory record schema — phase-1 inventory synthesis (spike tg-hnlt)

Synthesized from seven read-only surface inventories: session bead lifecycle, step/molecule execution graph, transient event stream, review routes + gate minting, outward effects (GitHub), admission/locks/fencing, and live measurements of the ledger database (bd's state store — the human-tempo work ledger; measured on the reference deployment, a private station's month-old state store, the dolt database `tranquility`, whose name the specimen bead ids and store numbers below carry). Emitters cited exactly as reported; nothing here was independently re-derived.

---

# Transition catalog (draft)

Disposition legend: **T** = trajectory record, **L** = stays-bd (work ledger), **B** = both (trajectory record + a bd-facing fact survives), **R** = retires outright. Rows marked **⚠** carry a reader disagreement (detailed beneath each family).

## Family 1 — Attempt lifecycle (started / heartbeat / terminal)

| Proposed record | Today's emitter(s) | Payload fields | Correlation keys: have / missing | Today's persistence | Disposition |
|---|---|---|---|---|---|
| `attempt.started` (session mint) | `grid_runtime/lib/src/lifecycle/station_bead_writer.dart:295-327` (`createSession`); sole caller `grid_engine/lib/src/circuit/session_scope.dart:634-639` | rig, work_bead, started_at, grid.session.model=molecule | have: bead, session / missing: worktree, branch, attempt id, admission-grant ref | bd create + stamping merge (session bead); flare `session.minted` (`:325`) **dropped** — no onFlare on state writer (`work_assembly.dart:484-489`) | B |
| `attempt.process.started` / `attempt.process.terminal` | `grid_runtime/lib/src/runtime/subprocess_provider.dart:286` (SessionStarted), `:447` (event add), `:463-490` (terminal classification: Exited/Died/Respawned, `inferred` flag); typed `runtime_event.dart:18-83` | pid, pgid, beadId, exitCode, **inferred**, reason, epoch, deadline | have: session (runtime name), bead, pid/pgid / missing: attempt id, step, durable anything | in-memory broadcast stream only — the raw observation (esp. `inferred`, `Died.reason`, `Respawned.epoch`) is never persisted | T — the closest observer-written attempt-lifecycle source that exists |
| `attempt.identity.stamped` (DEAD PATH) | `grid_engine/lib/src/domain/session_bead.dart:484-492` (`startedIdentityMetadata`) — **no live caller**; `grid_runtime/lib/src/lifecycle/runtime_actuator.dart:107-296` — **never constructed outside tests** | pgid, pid, token, state machine (start_pending/…/quarantined), crash_count, worktree, branch | schema slot for worktree/branch exists here and only here — unwired | nowhere (historical beads may carry keys; `staleFences` still reads them, `session_disposition.dart:127-147`) | T — the hole the decision fills |
| `attempt.lease.acquired` / `.released` / `.swept` | `grid_engine/lib/src/molecule/process_lease_vendor.dart:832` (acquire breadcrumb), `:872` (release blank-sentinel clear), `:667` + `:566-710` (boot orphan sweep) | grid.lease.pgid/pid/token; sweep: disposition (killed/refusedUnsafe/leftAdoptable), terminateResult, clearFailure | have: step bead, session (via grid.step.session), token (de-facto attempt id) / missing: attempt ordinal, authority, history (overwritten in place) | bd metadata merge on step bead; sweep outcomes are onOrphan log lines + in-memory `SweptLeaseGroup` | T |
| `attempt.adopt.proved` | `process_lease_vendor.dart:751-775` (`adoptable` via `metadataOf`); flat twin `sdk/allocation.dart:593-626` (`startOrAdopt`), fence from `capability_host.dart:340-344` | AdoptFence{pgid,pid,token}, boolean outcome | have: step, session, pgid/token / missing: everything durable | in-memory decision; adopted-vs-respawned recorded nowhere | T |
| `attempt.heartbeat` | **NO EMITTER EXISTS** (unanimous across all seven readers) | — | — | nothing; liveness inferred from worktree `.grid` mtimes + projected `grid.step.state` ("/status and wedge lie") | T — net-new capture |
| `attempt.terminal.succeeded` | `session_scope.dart:904-970` (`_completeAndClose`): outcome stamp `:908`, reap `:915`, worktree reap `:920-955`, close `:957` (closed_at at `station_bead_writer.dart:1226-1236`), gate sweep `:959` | grid.outcome=complete, closed_at, gate close_cause=session-terminal | have: session, bead, delivery receipt (via step grid.result.*) / missing: worktree id (reaped by path convention), commit digest | four-step mutating tail on session bead, each step LOUD-non-fatal (the tg-tlea crash window) | B (trajectory terminal + slimmed head-summary status) |
| `attempt.terminal.escalated` | `session_scope.dart:1341-1365` (`_escalateAndClose`) | grid.escalation=breaker-exhausted, grid.escalation_reason, closed_at | have: session, bead / missing: step (in reason prose only — unstructured) | metadata merge + bd close | B (terminal → trajectory; HELD human-owned state → ledger) |
| `attempt.terminal.lost` (void retire) | `session_scope.dart:578-585` writing `voidRetireMetadata` (`session_bead.dart:304-311`); fence probe `:740-746`, refusal `:752-763` | work_bead REWRITTEN to `<bead>#void-<deadSession>` (`rework.dart:94-95`), grid.voided_reason | have: bead, session (dead) / missing: original join key survives only inside the mutated string | metadata merge that **mutates the join key**; flares `session.voided` `:595` / `session.voidRefused` `:752` transient | T |
| `attempt.terminal.settled` (work closed externally) | `station_bead_writer.dart:439-477` (`settleSessionForTerminalWork`); callers `restart_reconciler.dart:813`, `work_list.dart:363-371` | grid.outcome=complete, grid.work_terminal_reason, closed_at | have: session, bead; receipt typedef in-memory only (`:145-149`) | merge + close; flare `session.workTerminal` `:467` **dropped** (null sink) | T |
| `attempt.round.retired` (rework re-key) | `grid_sdk/lib/src/command/station_command_handler.dart:320-323` (work_bead → `<bead>#r<N>`, `rework.dart:44`); spec clear `:319`, note append `:333-337`, reap `:348` | session work_bead re-key; work-bead note + spec clearing | have: bead, session, round N (inside a mutable string) / missing: first-class (bead, session, round) tuple | join-key mutation + note append + molecule reap | B (re-key event → trajectory; operator note/spec → ledger) |
| `attempt.round.closed` (retired-rework close) | `session_scope.dart:211-225` → `station_bead_writer.dart:331-343` (close + gate sweep, cause superseded-round); reap-first `:531-536` | closed_at, close reason='reworked', gate close_cause=superseded-round | have: session, bead (via #rN key) | merges + bd close | T |
| `attempt.rework.declined` (orphan guard) | `session_scope.dart:1321-1339` (`_declineRework`) | grid.rework_declined=true + reason | have: session only | metadata merge; makes HELD | B |
| `attempt.mint.failed/exhausted/abandoned/refused` | `session_scope.dart:817/:827` via `_flareMint :840`; `:786` (`_stopAbandonedMint` with **stage** field); `:475/:510` (`session.mintRefused`) | workBeadId, attempt, maxAttempts, reason, stage, snapshotCapturedAt | have: bead, attempt counter (in-memory, resets on bounce) / missing: durable anything — no session bead exists on the mintFailed path | flare-only | T |
| `attempt.teardown.replayed` | `restart_reconciler.dart:570-634` (`replayTeardownTail`) + `:644-722`; candidates `station_bead_writer.dart:989-992`, `:724-744` | replay of the terminal tail | have: session, bead, worktree | bd closes + merges at boot | **R** — exists only because the terminal tail is a non-atomic multi-write; the decision's falsification clause retires the whole replay class |

## Family 2 — Admission grants (issued / consumed)

| Proposed record | Today's emitter(s) | Payload fields | Correlation keys: have / missing | Today's persistence | Disposition |
|---|---|---|---|---|---|
| `admission.granted` (pending→mounted) | `grid_engine/lib/src/seeds/work_list.dart:436-508` (governor slots `:451-475`, `_mountedIds.add` `:482-508`) | bead id, slot accounting | have: bead, session (when adopting) / missing: **reservation id, fencing token, authority id, expiry, basis revisions — every tg-y4fd field** | **in-memory set only** — evaporates on bounce | T — the exact transition tg-y4fd turns into a grant |
| `admission.attempt.recorded` | `station_bead_writer.dart:535-575` (`recordMountAttempt`); caller `work_list.dart:146-174` | grid.attempt.work_bead, grid.attempt.count (mutated in place), grid.attempt.last_at, per-attempt appended notes | have: bead, attempt ordinal (count only) / missing: session (attempt precedes mint), attempt identity, decision basis, terminal outcome | ONE mutable `mount-attempt` bead per work bead (anti-amplifier by design); write failure flares and does **not** block admission | T — subsumed by the grant record (unanimous, 5 readers) |
| `admission.refused` / `.restored` (eligibility) | `work_list.dart:305-312` → `:550-588`; policy `power_station grid_assets/lib/src/code/mount_eligibility.dart:5-26`; engine clause `domain/mount_attempt.dart:122-152` | beadId, clause (first refusal wins; incl. attempt-cap clause) | have: bead / missing: snapshot revision, authority | flare-only, edge-triggered on an in-memory latch lost per boot | T |
| `admission.trust.refused` | `work_list.dart:314-327` + `:632-653`; guard `bridge/trust_guard.dart:23-82` | beadId, origin, floor, reason | have: bead | flare-only, once per bead per process lifetime | T |
| `admission.throttled` | `work_list.dart:509-511, 597-606` | count, beadIds | have: bead list | flare-only per build | **R/drop** (unanimous: derivable from the fold — ready minus granted) |
| ⚠ `admission.held` | `work_list.dart:386-387, 614-630` | beadId, sessionId, reason | have: bead, session | flare-only, dedup set lost on restart | ⚠ drop (event stream reader: derivable from durable session hold markers) vs both (admission reader: hold markers ledger + refusal event trajectory) |
| `admission.mount.declined` (I-10) | `session_scope.dart:401-431` (`_declineMount`, microtask-scheduled to guarantee the flare) | workBeadId, sessionId, reason | have: bead, session | flare-only; scope goes inert | T |
| `admission.void.refused` (live-orphan fence) | `session_scope.dart:753` (`_refuseVoidMint`) | workBeadId, deadSessionId, pgids, reason | have: bead, session, pgid fence | flare-only — the sole record; operator must intervene | T |
| `authority.lock.acquired` | `grid_cli/lib/src/up_command.dart:414-431` → `station_lock.dart:82-133`; pgid==pid asserted (03b0c29) | pid, pgid, startedAt | have: pid/pgid, grid home / missing: fence/epoch, bead/session keys | `.grid/station.lock` (exclusive-create, 0600) — rewritten in place, deleted on release; transitions themselves nowhere durable | B |
| `authority.lock.stolen` / `.refused` / `.down` | `station_lock.dart:137-170` (`_probeAndSteal`), `:150-157` + `up_command.dart:432-436` (exit 64), `station_attach.dart:254-291` (stop/SIGTERM path) | holder pid/startedAt, steal reason (stale/corrupt), recorded-vs-actual pgid, fallback reason | have: pid only / missing: fence — a steal reissues holdership with no monotonic counter (tg-o2fy hazard) | stdout log line + file delete/refusal; nothing durable | T |
| `authority.lock.advertised` (control/vm) | `station_lock.dart:212-219` (`updateControl`), `:225-229` (`updateVmService`) | controlUrl, bearer token, vmServiceUri | pid/pgid | rewrites lock file in place | unsure (single reader) — likely stays a file; fold must not depend on it |
| `admission.drive.approved` | `work_list.dart:290-296` (driveList check); config `grid_sdk/lib/src/work/station_work.dart:142-153` | approved bead-id set | have: bead / missing: everything — the approval is a command-line argument | in-memory for the boot | T (grant decision-basis input) |
| `admission.human.approval` (grid.approved / validation_plan / type) | no code writer — humans/skills via bd label+metadata; only reads at `mount_eligibility.dart:5-26` | label, validation_plan, issue type | bead; missing: revision snapshot at grant time | bd work ledger | **L** — stays bd; the grant must snapshot its revision |
| `admission.link.blocks` (cross-store) | `grid_cli/lib/src/link_command.dart:242` → `station_bead_writer.dart:483-513` (`createLink`) | grid.link.from/to/type/reason/actor | cross-store bead ids | bd bead create, append-shaped (28 rows measured) | **L** (unanimous) — grant authority consumes it from bd |
| `federation.lease.granted/beat/reaped` | `power_station federated_grid_assets/lib/src/lease_manager.dart:67-179` (monotonic `_fence` `:139`); wire `federation_protocol.dart:128-226`; fence validated `station_server.dart:176-218` | leaseId, fencingToken, ttl/heartbeat, kind, lessee, idempotencyKey | have: leaseId, fencingToken, station, step (lessee-side result merge `federation_protocol.dart:261-266`) | owner side **in-memory only** (restart forgets all grants); lessee side durable in grid.result.* | T — the only tg-y4fd-shaped grant in the codebase today |

## Family 3 — Verification bindings

| Proposed record | Today's emitter(s) | Payload fields | Correlation keys: have / missing | Today's persistence | Disposition |
|---|---|---|---|---|---|
| `verify.verdict.recorded` (critic lane) | the critic **agent itself**, per contract at `power_station grid_assets committee.dart:1200-1248` (prompt), `:368-379` (atomic write), `:341-365` (stamp) | {rubric, version, grade A–F, rationale, nodePath, round} → `.grid/critique/<rubric>.json` | have: bead+step (nodePath), round / **missing: session id, commit digest** | worktree file, swept per round (`sweepStaleCritique :561-591`), dies with worktree reap; durable copy = grade/transport/rationale merged onto step bead | T — self-reported today; decision requires observer-written |
| `verify.gating.rc` | sh wrapper `_gatingScript` `committee.dart:1404-1405`; spawned `:954-962`, 600s cap `:126` | raw exit code, one line, **no stamp of any kind** | missing: everything — nothing records which tree state the plan ran against | worktree file, read at `:1092-1113` (A iff '0' else F; fail-closed-default when absent) | T |
| `verify.scope.pinned` (diff + digest capture point) | `PinDiffCapability` `committee.dart:645-808`; `_writePinnedDiff :768-792`; guards `:671-695` | file: branch, baseRef (**ref name**, not SHA), `git log --oneline` short SHAs, full diff; durable Advance payload: {base, commits=**COUNT**, diffBytes} | have: bead (branch), round / **missing: any immutable digest in the durable half** | transient file (wiped per round) + step-bead result merge | T — cheapest place to capture the digest the decision requires (it already runs git at the right instant) |
| `verify.verdict.recovered` | `probeCompletionArtifact` `committee.dart:881-940` (canonical rewrite `:914-925` from stdout envelope) | re-synthesized {grade, rationale, nodePath, round} | step, round; provenance only as `transport=envelope` inside the payload it qualifies | rewrites the agent artifact on disk | T (recovered-not-authored must be a first-class fact) |
| `verify.route.verdict` | `CodeRouteCapability.route` `committee.dart:1289-1375`; persisted via `capability_host.dart:650-712 / 594-604`; spec twin `respec.dart:487-560` | route_verdict=advance\|escalate, grades CSV, spread, rule; fail-closed missing-grade→F `:1315-1318` | have: bead, session, step, round (implicit via incarnation bead) / missing: which transport/round each joined grade came from at route time; commit | one chokepoint merge on the route step bead | T |
| `verify.usage.telemetry` | harness envelope redirect, `committee.dart:1011`; path codec `usage_report.dart:39-45` | tokens in/out, cost, premiumRequests, num_turns, duration, actual model | step (sanitized nodePath); round only via merged copy | `.grid/telemetry/<node>.usage.json` + `readUsageFields` merge into step result `:1143-1144` | T (already trajectory-shaped, FT-2 capture-only) |
| `verify.completion.fence` | `sdk/allocation.dart:726-732` → `_settleInferredCompletion :774-812` → `_probeWorkSignal :829-848` | GateOutcome clear/present/probeError (fail-closed) | step, session, worktree probed / missing: which commit the completion binds to | probe outcome lands **nowhere**; only its consequence (step state) persists | T |
| `verify.result.persisted` (grid.result.* generally) | `capability_host.dart` persist paths; `sdk/route.dart:49,219`; measured shape reader 7 (grades 1,585 rows, rationale 1,188, pr_url 50, base='origin/main', commits='1') | grade/model/round/cost/tokens/rationale/transport/verdict/lane/rule/discovery keys/deliver keys | bead, session, step, delivery receipt (pr_url) / **missing: any SHA — zero digests in all 7,258 surviving rows** | step-bead metadata merges, reaped at session close | T |
| ⚠ `verify.operator.ruling` (gate resolve) | `station_command_handler.dart:507-533` (`operatorRulingMetadata` onto SESSION bead, `session_bead.dart:250-260`; gate stamped adjudicated + closed `:519-533`) | per-lane grade, transport=operator-ruling, rationale, evidence_session | gate, session, lane path | metadata merge on the session bead (which is slated to slim) — and **operationally dead**: bd rejects `--if-assignee` (tg-j0o0) | ⚠ ledger (gates reader: it's the human approval record) vs unsure (session reader: needs an explicit home once the session bead slims) |
| `verify.ci.concluded` | `github_reconciler.dart:162-243` (`_feedback`; head SHA read at `:186` then **dropped**) | nodeId, actor, repo, observationId, headBranch, checkName, conclusion | have: bead (from `grid/<bead>`), observation id / missing: commit (read then discarded) — CI conclusions cannot be bound to what was verified | in-memory event; per-PR etag saved; merged/closed-PR checks never observed | T |
| `verify.ci.landing_ready` | `ci_feedback_projection.dart:145-157` (`grid.landing_ready=true`) — **DORMANT**: no production provider for `CiFeedbackProjection` (`github_grid_assets.dart:56`) | grid.landing_ready=true, never cleared | bead; check identity only in idempotency key | bd metadata write on the work bead — **zero readers exist** | T (or retire; open question) |

## Family 4 — Effect intent / acknowledgement (with explicit unknown)

Structural fact (outward-effects reader): **no effect intent is recorded before any outward mutation**, and `StepOutcome` is a closed Ok|Failed — "unknown" is unrepresentable; every row below is today ack-only or nothing.

| Proposed record | Today's emitter(s) | Payload fields | Correlation keys: have / missing | Today's persistence | Disposition |
|---|---|---|---|---|---|
| `effect.commit.residue` | `github_pr_delivery.dart:46-55`, `github_direct_merge_delivery.dart:41-50` → `git_ops.dart:427-434` | conventional subject + bead-id git trailer | bead (trailer), worktree / missing: the commit SHA (exists in git, never extracted) | git object only; no grid-side record | T |
| `effect.push` | `github_pr_delivery.dart:108-118` (`--force-with-lease`), `github_direct_merge_delivery.dart:55-64` | branch `grid/<bead>`, remote, failure tail-400 | bead (branch name) / missing: pushed HEAD sha, intent record, timestamp | remote ref mutated; success only implied by the later receipt; no timeout set | T |
| `effect.pr.opened` | `github_pr_delivery.dart:121-144` → `github_app_pr_opener.dart:49-60`; reuse `landing.dart:370-376` | title, body (from `.grid/pr-body.md`), head, base → Ok{pr_url, reused} | bead / missing: PR **number** (parsed `:84-89` then discarded); reused path can persist **empty pr_url** (`:140-142`) | ack via `effect.terminal.receipt` only; timeout-with-created-PR healed by retry, never recorded as unknown | T |
| `effect.automerge.enabled/fallback` | `github_auto_merge_delivery.dart:31-48`; gate `github_delivery_policy.dart:30-57`; fallback flare `:55-62` | auto_merge=enabled\|fallback-pr-no-merge + reason | bead, pr_url, grades (inputs) / missing: policy version under which it ran | receipt keys + transient flare | T |
| `effect.directmerge.landed/refused` | `github_direct_merge_delivery.dart:70-100`; protection probe `github_merge_runner.dart:82-101`; refusal flare `:102-117` | direct_merge=landed, branch, base | bead, branch, base / missing: base sha | the **only** effect whose unknown (unreadable protection) maps to refuse rather than blind retry | T |
| `effect.terminal.receipt` | `capability_host.dart:650-712` (`_persistAdvance` → `_persistComplete :594-604`, ONE write) | state=complete + timing + route_verdict + delivery method id + pr_url/reused/auto_merge + pr_title/validation_rc/committee_grades | bead, session, step, receipt (pr_url) / missing: per-effect timestamps — commit+push+PR+auto-merge collapse into one write; a cancel racing completion **drops the ack entirely while the PR stands** (`:700`) | step-bead merge — today's entire effect-acknowledgement record | T |
| `effect.failed` | `capability_host.dart:615-633` (`_persistFailure`); reached from Failed/throw/delivery-fail/persist-recovery `:473-482` | state=failed, restartCount+1, cooldownUntil, failureReason tail | bead, session, step, attempt only as mutable counter / missing: store-unavailable vs work-failure distinction (conflated, tg-7ux ambiguous-write documented at `:415-418`) | step-bead mutation per retry (the 81%-commit churn path) | T |
| `effect.unarmed` | `capability_host.dart:664` (deliver.unarmed flare) | posture: commit-only completion | session, step | flare-only | T |
| `effect.observation.claimed` | `github_reconciler.dart:245-265` (cursor.record + save **BEFORE** emit — at-most-once; permanent loss for CheckConcluded) | observationId (`poll:issue:…` `:111` / `poll:check:…` `:221`) | observation id, repo, substation | cursor file `.grid/github/<owner>-<repo>.cursor.json` (`github_reconciler_binding_assets.dart:44-51`), observationIds capped 512 | T (claim/loss must become explicit) |
| `effect.poll.cursor` | `github_reconciler.dart:81-160` + `file_cursor_store.dart:41-50` | since watermark, etags, observationIds | substation, repo | file, whole-file replace | **L**-ish (stays a cursor file per reader; fold does not own it) |
| `effect.intake.upserted` | `github_intake_projection.dart:19-71` → `github_intake_store.dart:50-103` | deferred chore bead, external_ref `github:<nodeId>` | external ref, repo, actor | bd create/update — genuine work-ledger material | **L** |
| `effect.ci.rework.commanded` | `ci_feedback_projection.dart:73-91` → `resident_feedback_command.dart:44-114` (POST /command, X-Grid-Fence=wall-clock micros, Idempotency-Key) | beadId, note, idempotencyKey `github-ci:<bead>:r<N>:<check>:<obs>` | bead, round, check, observation / missing: server dedupe is in-memory (`station_control.dart:410-465`) — lost on restart; ignored conclusions (`ci_feedback.dart:50-54`) silent and unrecorded | transient + at-most-once upstream claim | T |
| `effect.ci.capgate.minted` | `ci_feedback_projection.dart:159-194` (deterministic id `<bead>-ci-rework-cap`) | gate bead, blocks, reason with check identity as prose | bead, session / missing: typed check correlation | bd gate create, idempotent | **L** |
| `effect.pr.body` | `delivery.dart:143-156` (`.grid/pr-body.md`) | composed PR body | bead, worktree | worktree file, read-back at delivery | **R/drop** |
| `effect.binding.derived` (delivery posture) | `github_grid_assets.dart:51-124`, `composition_assets.dart:124-168`, `substation_seat.dart:173-215` | ServiceBundle{sourceControl, delivery, …} | substation | in-memory composition; the posture under which an effect ran recorded nowhere | **R/drop** (posture should be snapshotted into the effect record instead) |

## Family 5 — Step-and-molecule transitions

| Proposed record | Today's emitter(s) | Payload fields | Correlation keys: have / missing | Today's persistence | Disposition |
|---|---|---|---|---|---|
| `step.instantiated` (pour) | `session_scope.dart:633-681` / `:1069-1104` (orphan resume) → `molecule_codec.dart:270-294` → `station_bead_writer.dart:758-804` → `beads_dart bd_cli_service.dart:469-489` (`bd create --graph`, one txn; per-edge cycle CTE server-side — the tg-ehht timeout site) | molecule + step beads (grid.step.id/capability/kind/path/session/swarm; grid.circuit.formula/session), parent-child/blocks/validates edges | bead, session, step / missing: per-record order within the batch (one dolt commit ≈ 6k beads across 567 pour commits, measured) | bd graph-apply, dedup-probed (`:1382-1383`) | T |
| `step.crumb` | `station_bead_writer.dart:806-837` (`_stampMoleculeCrumbs`) | bead-id chain workBead/session/molecule/…/step | full breadcrumb chain | one bd update per node (N extra commits per pour) | T (pure correlation material — trajectory carries it natively) |
| `step.running` | `capability_host.dart:551-568` (`_persistStarted`, from AllocationStarted `:359-366`) | grid.step.state=running, startedAt | session, step / missing: attempt id (GRID_INSTANCE_TOKEN minted `:207` and dropped) | step-bead mutation, prior value only in dolt row history | T |
| `step.ready` | `capability_host.dart:577-589` | state=ready, timing triple, optional rendezvous payload | session, step | step-bead mutation + flare | T |
| `step.complete` | `capability_host.dart:594-604`; terminal-advance variant `:650-712` | state=complete, timing triple, result keys | session, step, receipt / missing: commit digest | one atomic merge | T |
| `step.failed` (supervised retry) | `capability_host.dart:615-633`; loop via `frontier.dart:118-120` + `circuit_scope.dart:160-165` (ValueKey `$path#$restartCount#g$round`) | state=failed, restartCount+1, cooldownUntil (omitted at exhaustion — **stale prior value survives** by merge semantics), failureReason, timing overwritten per attempt | session, step, restartCount (counter, not identity) | ≥2 dolt commits per retry per step; measured: top-12 storm beads = 77% of 544,956 commits, worst single bead 149,420 commits/~84h (`tranquility-8krit`, SessionClosedRefused loop); a live specimen open now (`tranquility-o0wexi`) | T — attempt granularity resolves exactly here |
| ⚠ `step.gated` | `capability_host.dart:90-130` (`persistRaisedEscalation`; step write `:116` **before** gate mint); derived twin `session_scope.dart:1004-1030`; gatedMetadata `:737-742` | state=gated + route_verdict=escalate + timing; then separate gate bead | bead, session, step | two non-atomic writes | ⚠ trajectory (gates reader: step half) vs both (graph + event-stream readers: counting the gate bead) — substantively agreed: step write → T, gate bead → L |
| `step.rearmed` (D-7) | `session_scope.dart:1222-1274` (`_rearm`, single-key `{grid.step.state: pending}` `:1260-1263`); scheduled `:1589-1600` | state=pending only (deliberately minimal) | session, step | step-bead mutation; **stale-join mechanic**: a bare gate close does not bump the round, so old on-disk verdicts re-join and re-gate (the I-14 loop; matches "readiness gate-close is also stale") | T |
| `step.superseded` (A52 successor) | `station_bead_writer.dart:881-935` (`createStepSuccessor`: create, dep **first** `:931` — tg-q3q0 — then merge); caller `session_scope.dart:1128-1205`; budget 5/path, exhaustion parks `:1191` | new step bead (identity keys copied, state/telemetry/results stripped), supersedes edge, rewritten crumb, state=pending | session, step path, round = chain depth (derived, never recorded as a number) | 3 non-atomic writes; the one already-append-only structure on this surface (529 supersedes + 301 validates edges measured) | T |
| ⚠ `gate.opened` / `gate.regated` | `station_bead_writer.dart:606-676` (`createGate`: fresh mint `:651-666`; dedup refresh `:628-649` mutating reason + regate_count + regated_at under CAS); callers `capability_host.dart:118`, `session_scope.dart:709/:1191` via `parkSessionAtGate :712-722` | title `grid gate <session>@<node>`, rig, blocks=sessionId, node, reason (avg 1.0KB prose measured); on reuse: count++/timestamp reset, **reason clobbered** | session (blocks), step (node path) / missing: step bead id, round at gating, per-cycle regate history | bd gate bead; flare `gate.opened` **dropped** (null sink) | ⚠ both (session/event/gates/admission readers) vs ledger (store reader for the mint) and vs pure-trajectory (gates reader for the regate) — net proposal: gate bead L, open/regate/close transitions T |
| ⚠ `gate.closed` (sweep/hand/adjudicated) | `station_bead_writer.dart:400-437` (`_closeEligibleOpenGates`: cause CAS merge then close; eligibility `:357-398`; straggler `:678-701`); entries `:331-343`, `:345-355`, `restart_reconciler.dart:699` | grid.gate.close_cause ∈ {session-terminal, work-bead-closed, superseded-round, duplicate-mint, straggler-route, adjudicated, unclassified `:92-103`}; measured: 554/601 closed gates have **no** close_cause | session, gate id, node / missing: close actor, open duration | merge + close; `GateAutoCloseReceipt` in-memory only; flare dropped | ⚠ both (four readers) vs ledger (store reader) |
| `molecule.reaped` | `station_bead_writer.dart:994-1032` (`reapMolecule`: collect `:1481-1493`, supersedes fixpoint `:1390-1405`, topo order `:1416-1471`, batch close with per-bead proxied fallback `:1017-1031`); all session exits + `station_command_handler.dart:347-351` + boot replay | bd close on ~35 beads/session, reason='molecule reaped (session close)' | session, step, bead | mass status mutation; residue measurable now (56 open steps all under closed sessions) | **R** (unanimous, 3 readers) — the decision's named falsification criterion |
| `reap.backfill.hard-delete` | the measured deployment's `tool/reap_backfill.sh:7` (`bd delete --force`) | hard delete of issues **and their events rows** (13,775 rows = 65% of all beads ever minted, 2026-08-25) | bead (recoverable only via `dolt_history_issues`) | 92 dolt delete commits; none of the 18GB of generated history reclaimed | **R** |
| `flare.*` (the full lifecycle vocabulary) | `capability_host.dart:769-779`; `session_scope.dart:768-774,840-851`; `work_list.dart:577-653`; `wedge_monitor.dart:99/:121`; `station_bead_writer.dart` sites (all **dropped** — null sink `work_assembly.dart:484-489`); production sink `space_station_assets up_command.dart:488` → `diagnostics_reporter.dart:36-50` (30s rate limit with a bucket key that mismatches most payloads `:31-34`) | name + flat string map (sessionId/workBeadId/nodePath/reason/attempt counters); **no timestamp, sequence, station identity, or boot epoch** (`capability.dart:510-517`) | session, bead, step, attempt counters | transient only; `DurableStationEventLog` (`grid_cli station_event_log.dart:53-122`, cursored JSONL, typed decode) exists and is **wired nowhere** | T — these ARE the trajectory events in embryo, currently evaporating |
| `wedge` / graph-event ring / tree snapshots / `/status` / bd events rows / per-write dolt commits / sync flares / critique sweep | `wedge_monitor.dart:88-123`; `beads_repository.dart:71-93` (256-ring); `tree_projector.dart:28-34` + `station_control.dart:362-399`; `station_control.dart:191-214` + `up_command.dart:729-781`; bd events table (188,992 updated rows, **all with empty old/new value** — measured); 544,956 dolt commits/18GB; `work_assembly.dart:421-435`; `committee.dart:514-544` | various | — | derived / content-free / storage pathology | **R/drop** (unanimous where multiple readers covered them) — pure projections to be re-served from the fold, or defects the split removes |
| `note.appended` (registry sink) | `work_assembly.dart:522-532` → `StationBeadWriter.update(appendNotes)` | appended note line | bead or session | bd note append | unsure (single reader) — content-dependent; open question |
| `worktree.provisioned` | `sdk/allocation.dart:650-661` → `code_capabilities.dart:610-671` → `station_git_service.dart:309-358` (`git worktree add -b grid/<bead> … <root.defaultBranch>`; pre-existing branch **adopted** with no re-cut `:335-341`) | BeadWorktree{beadId, path, branch} — in-memory return only | bead, worktree / **missing: base commit digest, cut timestamp — nothing lands in any store** | filesystem + git branch only; scar tissue: tg-c8s, pow-1g7, tg-kb3z | T |
| `worktree.reaped/held/failed` | `session_scope.dart:934/:939/:949` | uncommitted/unpushed/stashes counts, reason | session, bead / missing: worktree path & branch **not in the payload** | flare-only | T |

---

# The fold's query contract

Every read the station serves today off session/step/molecule/gate state. Proposed projection-table families: **P1 attempt/session-head**, **P2 step-cursor** (per-attempt step state + timing), **P3 admission** (frontier, grants, budgets), **P4 gate** (open gates + cycle history), **P5 verification/results**, **P6 process-identity** (leases/fences), **P7 telemetry/metrics**. Reads that stay on bd or on files are marked as such.

| # | Query | Consumer | Fields needed | Served by |
|---|---|---|---|---|
| 1 | sessions joined to work beads (sessionsByWorkBead) + open gates + molecule buckets | `station_join_bridge.dart:206-239, 313-338, 360-398`; `projectSession` `session_bead.dart:436-474` | work_bead, status, grid.outcome/work_terminal_reason/escalation/rework_declined, session.model, pgid/pid/token, started_at/closed_at, grid.result.*, gate blocks/node, step-session stamps + dep rows | P1 + P2 + P4 |
| 2 | admission/mount frontier: disposition, blocksMount, staysMounted, retired-round fallback, attempt budget | `work_list.dart:330-433` + `:151` (mountAttemptsByWorkBead); full build read `:233-526` | sessionId, isTerminal, completed, humanHeld, workBeadId incl. #rN/#void shapes, cursor legacy fallback, grid.attempt.count/last_at, labels (grid.approved), validation_plan, grid.origin.*, driveList, caps | P3 (+ P1; approval facts read from bd) |
| 3 | adopt-or-mint + live circuit cursor + supersedes depth/round + gate re-arm + terminal detection for one session | `session_scope.dart` initState `:354-399`, build `:1403-1713` (`projectMoleculeCursor :1476`, results merge `:1525-1529`, `openGateNodes :1589`, depth `:1507-1515`, `staleFences`) | disposition inputs, moleculeBeads + dependencies, grid.result.* (session+step), openGateNodes, pgid/pid/token | P1 + P2 + P4 + P5 + P6 |
| 4 | pure disposition rule (done/held/voided/live/none), metadata-only variant | `session_disposition.dart:80-116`; `station_bead_writer.dart:125-136` (sweep eligibility `:364-383`, straggler `:680-693`, teardown replay) | isTerminal, escalation, rework_declined, outcome, cursor shape, work_bead | P1 (teardown-replay consumer retires) |
| 5 | wedge sample: live/running/gated/cooling | `wedge.dart:181-230` ← `wedge_monitor.dart:91`; `/status` wedge block | session terminality, per-step state, cooldownUntil vs now | P1 + P2 aggregate |
| 6 | `/status`: ready/mounted/liveSessions/perSubstation + wedge + sync + capturedAt | `up_command.dart:607-667` (producer `:729-781`); polled via `station_attach.dart:217` by status/gate/rework commands, watch scripts, station-operations | sessionsByWorkBead keys, isTerminal, graph.readyIds, wedge, sync stats/freshness | P1 + P3 aggregates (sync stays stream) |
| 7 | unclaimed frontier over live sessions (federation claim hook) | `unclaimed_frontier.dart:72-104` | isTerminal, workBeadId, sessionId, cursor | P1 + P2 |
| 8 | rework: THE session for a bead (single-valued join), parked-at-gate / pour-parked precondition, spent rounds via verdict evidence | `station_command_handler.dart:196-315`; `reworkVerdictEvidence` `session_bead.dart:353-394` | work_bead exact + #rN pattern over ALL sessions, isClosed, model, step buckets (state + grades + routeVerdict), open gate blocks | P1 + P2 + P4 + P5 — needs the first-class (bead, session, round) tuple |
| 9 | gate ls / gate resolve: open gates, regate age, feeding-F check | `station_command_handler.dart:378-416, 418-549`; `gate_command.dart:62`; gate-medicine skill | gate id/blocks/node/reason/createdAt/regate_count/regated_at, session lane grades/transport | P4 + P5 |
| 10 | boot reconcile: sessions backing surviving worktrees, settlement pairs, teardown-outstanding, closed-with-open-molecules, lease sweep candidates | `restart_reconciler.dart:746-837, 1042-1049, 570-634, 724-744, 881-898`; `sessionsAwaitingTeardown :989-992` | work_bead, isTerminal, outcome, held markers, step metadata verbatim (grid.lease.*), work-bead closed status | P1 + P2 + P6 (teardown-replay arm retires) |
| 11 | session ledger metrics: per-session cost/tokens/grades/false-F/round rollups | `session_ledger_metrics_projection.dart:534-650` (+ `:365-391, 499-501, 557-580`) | started/closed_at, work_bead, grid.result.* (session+step), step timing triple, supersedes depth | P5 + P7 |
| 12 | chokepoint safety re-reads (gate-mint refusal on closed session, sweep eligibility, settle guard, reap scan, mount-attempt dedup) | `station_bead_writer.dart:1353-1361, 364-383, 443-454, 1481-1493, 580-586` | isClosed, work_bead, held markers, session stamps, grid.attempt.work_bead | becomes the **fenced transition service's own fold reads** |
| 13 | prior-incarnation lease breadcrumb (adopt-or-respawn proof) + AdoptFence from cursor | `process_lease_vendor.dart:751-759` via `station_bead_writer.dart:1247-1257`; `capability_host.dart:340-344` | grid.lease.pgid/pid/token | P6 |
| 14 | eligible steps right now (deps positive-terminal, past cooldown, not broken) + incarnation re-key identity | `frontier.dart:133-152` → `circuit_scope.dart:145-187` (`:160-165` ValueKey) | state, restartCount, cooldownUntil, maxRestarts, supersedes depth as round | P2 (attempt identity becomes first-class) |
| 15 | ready frontier + terminal decision: invalidation closure, derived escalation, first broken node, completeness | `session_scope.dart` build via `molecule_codec.dart:72-213`, `live_frontier.dart:204-388`, `frontier.dart:164-234` | step states/paths/restartCount/cooldown, grades, delivery, supersedes + validates edges | P2 + P5 |
| 16 | mint dedup: already-poured / open successor exists | `station_bead_writer.dart:1382-1388` | open molecule/step by session, openSuperseding | transition-service fold (P2) |
| 17 | fresh-snapshot mint barrier | `session_scope.dart:504-520`; readers of graph.capturedAt | readyIds, capturedAt, isTerminal | P3 + bd snapshot version |
| 18 | cross-store link guard on the ready frontier | `station_join_bridge.dart:271-285` (`projectCrossLinks`) | open type=link, grid.link.from/type | bd read (links stay L), consumed by P3 |
| 19 | committee route join: sibling grades for the matrix | `committee.dart:1289-1375` via SiblingView | grid.result.<lane>.grade/rationale, critics/gating params | P5 |
| 20 | spec-committee freshness join: current-round on-disk verdict per lane | `respec.dart:487-560` | verdict JSON stamps, sibling round stamps, grid.round | P5 + verification records (replacing round-fence file reads) |
| 21 | critic completion-fence probe + result read | `committee.dart:881-940, 1067-1145` | rc, verdict stamps, pinned.diff, usage json | P5 |
| 22 | PR body composition: revalidate rc + route grades | `delivery.dart:158-166`; `landing.dart:298-310` | grid.result.<node>.outcome/grades/rc | P5 |
| 23 | auto-merge eligibility | `github_delivery_policy.dart:30-57` | validation_rc, committee_grades | P5 |
| 24 | current session + full rework-round ledger for CI feedback | `ci_feedback_projection.dart:35-58, 99-132` (via `bd export --all` — the banned/empty-on-proxied path) | session work_bead + #rN keys | P1 (fold fixes the export dependency) |
| 25 | harvest receipts: pr_url/pr_title/grades/rc per deliver step; pr_url as the human audit record | harvest-review flow; `session_bead.dart:177-223` doc (no programmatic reader exists) | grid.result.*.pr_url/reused/delivery/auto_merge | P5 |
| 26 | operator forensics: bd list -t session/gate, scoped step/molecule listings, dolt history archaeology, `/stream` flare tail | operator playbooks incl. `reap_backfill.sh:9-10`; `dolt_history_issues` (the only place update content survives — events rows are content-free) | ids, status, types, per-bead history | P1/P2/P4 reads + the trajectory table itself replaces dolt archaeology |
| 27 | live semantic tree per flush; VM-service graph-event ring | `/stream` (`station_control.dart:362-399`, devtools `websocket_tree_wire_source.dart:32`); `grid_controller_plugin.dart:91-127` over the 256-ring | tree walk, recentEvents, readyBeads, sync stats | stays live streams (not fold) |
| 28 | cursored durable event replay | `station_event_log.dart:107` (`replay`) — **no live caller** | cursor, timestamp, typed event | superseded by the trajectory read API |
| 29 | station-wide live count for the concurrency ceiling | `work_list.dart:470-474` | session isTerminal per work bead | P1 aggregate |
| 30 | supervisor liveness / attach: lock file reads | `station_attach.dart:259-306`; `station_lock.dart:137-157`; VM-service playbook | pid, pgid, startedAt, controlUrl, token, vmServiceUri | stays lock file (plus authority records in trajectory) |
| 31 | federation lease validity; sibling lease-claim record | `station_server.dart:176-218` + `lease_manager.dart`; `federation_protocol.dart:275-289` via SiblingView | leaseId, fencingToken, expiry; grid.result lease keys | P6 (owner state must become durable) + P5 |
| 32 | workspace path/branch/base derivation (no store read today) | `code_capabilities.dart:589-607`; worktree reap `session_scope.dart:927-951` | beadId, root config | deterministic fn + new worktree-provenance records (P6) |
| 33 | GitHub cursors, intake correlation, Idempotency-Key dedupe, station.lock control endpoint | `github_reconciler.dart:76`, `github_intake_store.dart:51-62`, `station_control.dart:410-465`, `resident_feedback_command.dart:50-59` | cursor doc, external_ref, key+fingerprint+fence, controlUrl/token | cursor file / bd / **fold should own command dedupe + fence** (currently in-memory, restart-lossy) |
| 34 | ready_issues / blocked_issues SQL views | ledger-database views (recursive CTE) → `bd list ready`, WorkList snapshots | status, type, blocks edges, defer, priority | stays bd (work-ledger frontier); state-store types vanish from it |
| 35 | grid.landing_ready | **NOBODY** (writer only, `ci_feedback_projection.dart:152`) | — | decide: kill or become an effect-ack record (open question) |

**Falsifier note:** rows 12, 16, 33's dedupe, and the in-memory admission latches (`_mountedIds`, `_mountEligibilityRefusals`, `_heldReported`, `_trustRefusedReported`, `_gateSweepsScheduled`, `_rearming`, mint/successor attempt counters) are state the fold must own durably, or the split cannot serve today's guarantees across restarts.

---

# The retirement list

Every bd write that STOPS under the split (for tg-zfek). Each item = the write class + its emitter(s).

**Step/molecule bead creates:**
1. Molecule/step graph pour — `station_bead_writer.dart:758-804` (`createMolecule`) → `bd_cli_service.dart:469-489` (`applyGraph`); callers `session_scope.dart:655-660`, `:1084-1089`.
2. Crumb stamps (one update per poured node) — `station_bead_writer.dart:806-837`.
3. Step-successor mint + supersedes dep add — `station_bead_writer.dart:881-935`; caller `session_scope.dart:1128-1205`.

**Step bead mutations:**
4. `grid.step.state` transitions (running/ready/complete/failed/gated) + timing triples + restartCount + cooldownUntil + failureReason — `capability_host.dart:551-568, 577-589, 594-604, 615-633, 726-746` and the dropped-persist recovery `:473-482`.
5. The gate-close re-arm single-key write — `session_scope.dart:1260-1263`.
6. `grid.result.*` merges onto step beads (grades, rationale, transport, route_verdict, delivery receipts, discovery keys, usage telemetry) — `capability_host.dart:650-712, 594-604`; `sdk/route.dart:49,219`.
7. Lease breadcrumb writes and sentinel clears — `process_lease_vendor.dart:832, :872, :667`.
8. Derived-escalation gated write — `session_scope.dart:1004-1030`.

**Session bead field churn** (bead survives as head summary written solely by the fenced service):
9. `startedIdentityMetadata` pgid/pid/token — `session_bead.dart:484-492` (already dead; delete).
10. Outcome/escalation/void/decline marker merges — `session_scope.dart:908, 1341-1365, 578-585, 1321-1339`; closed_at stamp `station_bead_writer.dart:1226-1236`.
11. work_bead join-key rewrites (`#rN`, `#void-`) — `station_command_handler.dart:320-323`; `session_scope.dart:578-585`.
12. Operator-ruling result merges onto the session bead — `station_command_handler.dart:507-518` (pending the operator-ruling home decision; path is dead per tg-j0o0 regardless).
13. Session-bead result copies (`grid.result.*` on sessions, 4 rows measured).

**The reap and its whole class:**
14. `reapMolecule` — `station_bead_writer.dart:994-1032` — and every call site: `session_scope.dart:915, :1354, :531`; `station_bead_writer.dart:462`; `station_command_handler.dart:347-351`; `restart_reconciler.dart:653, :672`.
15. Boot teardown replay — `restart_reconciler.dart:570-634, 644-722` (and its candidate queries `station_bead_writer.dart:989-992`, `:724-744`).
16. Per-graph `bd close` storms (6,318 'bd: close' commits measured) and the operator hard-delete backfill — `tool/reap_backfill.sh:7`.

**Admission bookkeeping beads:**
17. Mount-attempt bead create + in-place count/last_at merges + note appends — `station_bead_writer.dart:535-575`; caller `work_list.dart:146-174` (replaced by grant records).

**Consumer-less / defect writes:**
18. `grid.landing_ready` — `ci_feedback_projection.dart:145-157` (zero readers; retire or re-home explicitly).
19. The implicit per-write bd events rows and one-dolt-commit-per-field-mutation churn for all machine-tempo types (532,913 'bd: update' commits, 97.8% of history) — retires with the writes above.

**Stays in bd** (for contrast, so tg-zfek doesn't over-reap): work beads and their notes/spec fields, gate beads (create/close as human-facing items), link beads, intake chore beads, CI cap-gate beads, human approval facts (grid.approved/validation_plan/type), and the slimmed session head summary.

---

# Correlation-key gaps

Where today's writes lack keys the schema requires:

1. **Attempt id does not exist anywhere.** The de-facto identities are all unregistered or in-memory: `GRID_INSTANCE_TOKEN` (minted `capability_host.dart:207`, exported `:331`, carried by no flare and no durable write), the ValueKey `'$path#$restartCount#g$round'` (`circuit_scope.dart:160-165`), and the lease token (a fence with no issuer index). Restarts within a round overwrite the same step-bead fields; per-attempt lifecycle is dolt archaeology only.
2. **No heartbeat / liveness key.** Nothing is written between started and terminal on any surface; the `leases` table in the measured `tranquility` store is empty.
3. **No commit digest anywhere.** Measured: zero SHAs in all 7,258 rows' metadata; `grid.result.*.commits` is a count ("1"), `.base` is `origin/main` (mutable ref); pushed HEAD, residue commit, PR head sha all uncaptured (reconciler reads the head sha at `github_reconciler.dart:186` and drops it). Verification bindings are net-new capture — cheapest at PinDiffCapability and the completion-fence probe.
4. **No worktree/branch key on any record.** Derived by layout convention (`workspaceFor`) only; the worktree-reap flares omit the path; provisioning records no base commit or cut time.
5. **Round is derived, never recorded** — supersedes-chain depth computed per build (`session_scope.dart:1507-1515`); retired rounds recoverable only by `#rN`/`#void-` regex over mutable join-key strings. The fold needs a first-class (bead, session, round, attempt) tuple.
6. **Admission decisions carry no basis or identity**: no reservation id, no fencing token on the local write path (the only monotonic fence is the federation manager's in-memory counter, `lease_manager.dart:139`), no authority id (which supervisor/boot decided), no snapshot/bead-revision reference (grid.approved/validation_plan revision at grant time uncaptured), no idempotency key locally.
7. **Gate ↔ step incarnation is un-joinable from the store**: gates carry blocks(sessionId)+node but not the step bead id or gating round; re-gate history compresses to count+timestamp on a reused bead.
8. **Mount-attempt records carry no session key** (attempt precedes mint) — the counted set and the admitted set can silently diverge.
9. **No transition ordering, timestamps, or provenance**: flares carry no time/sequence/epoch (`capability.dart:510-517`); dolt batching collapses N transitions per commit and pours collapse ~6k creates into one commit; events rows are content-free (all 188,992 'updated' rows have empty old/new value); no observed-vs-reconstructed marker exists anywhere (single exception: `kSessionOutcomeLegacy`).
10. **Delivery receipts lack stable identity**: PR number parsed then discarded (`github_app_pr_opener.dart:84-89`); reused path can persist an empty pr_url; no per-effect timestamps; delivery posture/policy version unrecorded; check-run observation ids capped at 512 in the cursor.
11. **Exit-code provenance** (`RuntimeEvent.Exited.inferred` — murder vs completion identical on the wire) is consumed in-process and discarded.

---

# Open questions for the schema phase

Each stated as a decidable question:

1. **Attempt granularity.** Three retry ladders exist today — supervised restart within a round (restartCount, mutating), supersedes rounds (append, bead-per-round), mount attempts (counter, pre-session). Does the schema define attempt = one process incarnation (restartCount level), with round and mount-attempt as parent scopes — or fewer levels?
2. **Gate transition records** (⚠ reader conflict: both ×4 vs ledger ×1, and regate: trajectory vs both). Do gate open/regate/close land as trajectory records with the bead as a ledger view, and if so does the gate bead keep `blocks=<sessionId>` metadata or reference a trajectory attempt id?
3. **Operator-ruling home** (⚠ ledger vs unsure). The session bead it writes to is slimming and its code path is dead (tg-j0o0, bd rejects `--if-assignee`). Is the operator ruling a work-ledger approval record, a trajectory verification record, or deleted along with `gate resolve`?
4. **`work.held`** (⚠ drop vs both): is a hold-refusal event worth a trajectory row, or purely derivable from durable session hold markers?
5. **Registry note-append sink** (`work_assembly.dart:522-532`): split by content (work findings → bd, execution journaling → trajectory) — by what rule, decided where?
6. **Flare vocabulary as record-type seed.** Do the trajectory record types adopt the existing flare names/payloads near-verbatim (plus timestamp/sequence/attempt/authority), and does the state-writer null-sink hole (`work_assembly.dart:484-489`) get fixed by the transition service replacing that writer entirely?
7. **Head-summary contents.** Exactly which fields survive on the slimmed session bead (status? outcome? work_bead? closed_at?), and is the fenced service its sole writer including hand-close prevention (the `tranquility-8krit` 149,420-commit storm was caused by a hand-closed session bead)?
8. **Explicit-unknown representation.** One `unknown` terminal with a reason enum (write-timeout tg-7ux, cancel-raced ack `capability_host.dart:700`, inferred exit, dropped report `:360`, dropped breadcrumb, claim-then-deliver loss) — or distinct record types per ambiguity class?
9. **Effect intent records.** Is intent written per outward effect (commit/push/PR-open/auto-merge/direct-merge each) or once per terminal delivery? Per-effect is what makes the crash-mid-delivery window auditable; today all four collapse into one write.
10. **Verification digest capture points.** Confirm PinDiffCapability (base + HEAD SHA at pin time) and the completion-fence probe as the two digest emitters, and whether the delivery record additionally captures the pushed HEAD and the PR head/merge SHA.
11. **On-disk critic artifacts.** Do `.grid/critique/*.json`, the rc file, and usage json remain agent I/O whose contents are copied into observer-written verification records (making the round-sweep/stale-shadow machinery — tg-bns/tg-60t/I-14 — retire), or are they replaced outright?
12. **Fold ownership of in-memory state.** Which of the restart-lossy latches (admission `_mountedIds`, eligibility/held/trust dedup sets, mint & successor attempt counters, command Idempotency-Key map + fence `station_control.dart:434-456`) become projection tables vs stay ephemeral by design?
13. **Reconciler claim ordering.** Under trajectory records, does the GitHub observation claim flip to deliver-then-claim (at-least-once with idempotent projections), and does the CheckConcluded→CiFeedbackProjection arm get wired or deleted before migration (it is dormant in every production composition, `github_grid_assets.dart:56`)?
14. **`grid.landing_ready`**: retire the consumer-less write or re-type it as an effect-ack record with a defined reader?
15. **Local fencing token source.** tg-y4fd needs a durable monotonic fence: is it the transition service's own persisted counter, a dolt-backed sequence, or an extension of the station lock (which today has no epoch and is stolen without record)?
16. **Mount-attempt cap release.** A capped bead has no exit today (gate resolve dead; cure = re-file protocol). Does the grant schema model an explicit cap-release/grant-reissue record, or does the cap live purely in the fold with operator override?
17. **`DurableStationEventLog`** (`station_event_log.dart:53-122`, built, typed, cursored, never wired): adopt as the trajectory append prototype or discard — decide before tg-zfek deletes it as dead code.
18. **Backfill markers.** The schema needs an observed-vs-reconstructed field (A59-style archaeology, teardown replay, orphan sweeps have no vocabulary today) — enum values and who may write `reconstructed`?
19. **Trivia to reconcile during migration:** the cursor-file path discrepancy (`file_cursor_store.dart` doc comment vs actual `github_reconciler_binding_assets.dart:44-51` path) so no inventory keyed off the doc comment misses real files.
