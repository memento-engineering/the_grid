{
  "environment": {
    "dolt_version": "dolt version 2.2.2 (/opt/homebrew/bin/dolt; dolt warns newest is 2.3.1). macOS darwin 25.6.0 / arm64. Throwaway databases under /private/tmp/claude-501/-Users-nico-development-com-nicospencer-lunar-station/38816cc1-2114-42a2-9b64-a98433beca08/scratchpad/dolt-probe (db, db_trap, jsmall, jlarge, jrand, cad1, cad512). sql-server run on 127.0.0.1:13309 only, from that dir; killed at end (port clear, no probe process remains). The operator's existing per-repo bd sql-servers (ports 34951-54 etc.) were never contacted. Client = pymysql 2.2.8 in a scratch venv. Server defaults observed: @@transaction_isolation = REPEATABLE-READ, @@dolt_transaction_commit = 0."
  },
  "tests": [
    {
      "name": "T1 DDL fidelity",
      "question": "Does dolt 2.2.2 accept the draft's §4 DDL near-verbatim — ENUMs, JSON NOT NULL, DATETIME(6), five CHECKs, three UNIQUE keys, ten secondary KEYs, AUTO_INCREMENT BIGINT PK, plus traj_epoch / traj_terminal_guard / traj_pulse / proj_* tables?",
      "method": "Wrote §4 verbatim into 01_ddl.sql (dolt_ignore INSERT first, then traj_epoch, trajectory, traj_terminal_guard, traj_pulse, proj_meta, proj_session_head, proj_step_cursor) and ran `dolt sql < 01_ddl.sql` in a fresh `dolt init` db. Then `dolt sql -q 'show create table trajectory' -r vertical`. Later added the remaining §4 projections (proj_step_edges, proj_verification with DECIMAL(10,4)/CHAR(64)/TINYINT(1), proj_effects, proj_command_dedupe) via a heredoc.",
      "result": "Every statement accepted, zero syntax rewrites needed. SHOW CREATE TABLE round-trips all five named CHECK constraints (ck_prov, ck_terminal, ck_unknown, ck_provision, ck_grant), both ENUM columns, `payload json NOT NULL`, all DATETIME(6), all three UNIQUE KEYs and all ten KEYs, `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_bin`. 14 tables exist in the working set at the end. Note: dolt re-orders index declarations in SHOW CREATE (KEYs alphabetically, then UNIQUE KEYs) — cosmetic only. Note: a `dolt sql-server --data-dir <parent>` run plants a `.doltcfg` in the parent, after which CLI `dolt` inside a child db dir aborts with \"multiple .doltcfg directories detected\" until `--doltcfg-dir .` is passed — an operational trap for any tooling that mixes a data-dir server with per-db CLI calls.",
      "verdict": "pass",
      "implications": "§4 ships as written; no minimal rewrite required. The draft can drop any hedging about dialect fidelity. Add one operational note to §10.1: the transition service's server topology (single-db dir vs --data-dir) determines whether operator CLI calls need --doltcfg-dir, and the grid's existing proxied stores already run single-db-dir style."
    },
    {
      "name": "T2 CHECK enforcement",
      "question": "Are all five CHECK constraints actually refused at write time (terminal without outcome; unknown without reason; provisioned without commit_sha; grant without expires_at; non-observed without basis)?",
      "method": "02_checks.sh: one control INSERT of a fully valid attempt.terminal (must land), then one deliberately violating INSERT per constraint, then four bonus probes (payload NULL, bad ENUM literal, duplicate idem_key, duplicate (boot_epoch, epoch_seq)). Each via `dolt sql -q`. Final `SELECT seq, record_id, record_type, outcome FROM trajectory` to confirm only the control row survived.",
      "result": "5/5 refused, each naming the constraint: 'Check constraint \"ck_terminal\" violated', '\"ck_unknown\" violated', '\"ck_provision\" violated', '\"ck_grant\" violated', '\"ck_prov\" violated'. Bonus: payload NULL -> \"column name 'payload' is non-nullable but attempted to set a value of null\"; family='bogus' -> \"Data truncated for column 'family' at row 1\" (strict-mode refusal, not silent coercion to ''); duplicate idem_key -> \"duplicate unique key given: [terminal:ctrl]\"; duplicate (1,1) -> \"duplicate unique key given: [1,1]\". Surviving rows after the whole script: exactly one (the control).",
      "verdict": "pass",
      "implications": "§2.6 rule (6) holds — a codec-bypassing writer genuinely cannot record a terminal without an outcome, an unknown without a reason, a provision without a base SHA, or a grant without an expiry. The §12 dissent's mitigation for choosing the single-table shape over typed satellites is empirically real, not aspirational. Also: the ENUM columns fail closed under strict mode, so the closed `family`/`outcome`/`provenance` enums are safe as written; the §1 decision to keep `unknown_reason` an open VARCHAR rather than an ENUM remains the right call for a different reason (ALTER on versioned DDL), not because ENUMs are lax."
    },
    {
      "name": "T3 dolt_ignore",
      "question": "Do proj_% / traj_pulse tables registered in dolt_ignore before creation stay out of versioned history, survive in the working set across commits, and how do they behave under `dolt checkout .`, branch operations, reset --hard, and explicit staging (the §10.2 \"one-way trap\")?",
      "method": "INSERT INTO dolt_ignore VALUES ('proj_%',true),('traj_pulse',true) as the first statement, then created the tables and wrote rows. Ran DOLT_COMMIT('-Am',...), then `dolt status`, `dolt log`, `dolt_diff_summary('HEAD~1','HEAD')`, `SHOW TABLES AS OF 'HEAD'` vs `SHOW TABLES`. Then `dolt checkout .`; `dolt branch probe-b` + CLI `dolt checkout probe-b` + write + `dolt checkout main`; in-server `CALL DOLT_CHECKOUT('probe-b')`; `dolt add proj_meta`, `dolt add --force proj_meta`, `CALL DOLT_ADD('proj_session_head')`, `dolt_status` system table, `dolt reset`, `dolt reset --hard`. Trap test isolated in a second db (db_trap): force-add + commit an ignored proj_x, then observe status/-Am/recovery.",
      "result": "CORE: PASS. After DOLT_COMMIT('-Am') the status is \"nothing to commit, working tree clean\" despite proj_meta/proj_session_head/traj_pulse holding rows; dolt_diff_summary lists only dolt_ignore, traj_epoch, traj_terminal_guard, trajectory; `SHOW TABLES AS OF 'HEAD'` returns only the three versioned tables while `SHOW TABLES` (working) returns all seven. `dolt checkout .` left ignored rows intact. `dolt reset --hard` left ignored rows intact (proj_meta 2 rows, traj_pulse 1 row survived). `dolt add proj_meta` = silent no-op, rc=0, nothing staged; `CALL DOLT_ADD('proj_session_head')` = returns status 0 but stages nothing. BRANCHING — TWO DIFFERENT SEMANTICS: CLI `dolt checkout probe-b` CARRIES the untracked/ignored tables along (they appeared on probe-b, a row written there was then visible back on main), i.e. the projections follow whatever branch the CLI last checked out and are effectively single-copy. In-server `CALL DOLT_CHECKOUT('probe-b')` does NOT — the very next `SELECT ... FROM proj_meta` fails \"table not found: proj_meta\", because each branch has its own working set and only main's holds them. `SELECT ... FROM proj_meta AS OF 'probe-b'` also fails (never committed). ONE-WAY TRAP: real but requires deliberate force. `dolt add --force proj_x` stages an ignored table; after committing it, proj_x is tracked forever — status permanently shows \"modified: proj_x\", and DOLT_COMMIT('-Am') then REFUSES to stage it (\"nothing to commit\") because the ignore pattern still matches, so the working set can never be made clean and history holds a frozen stale snapshot. DROP TABLE + '-Am' also fails (\"nothing to commit\"). Recovery requires DROP TABLE + `dolt add --force` + commit, after which recreating the table returns it to ignored/clean.",
      "verdict": "pass",
      "implications": "§4's sequencing fix (ignore registration as statement zero) is validated and §10.3's backup-domain hole behaves as designed. THREE amendments the draft needs. (1) §10.2(c) overstates the trap for casual tooling and understates it for deliberate tooling: plain `dolt add`, `CALL DOLT_ADD`, `dolt add -A` and `DOLT_COMMIT -Am` all correctly skip ignored tables — only `dolt add --force` can spring it. But once sprung the failure is worse than \"tracked forever\": the working set becomes permanently dirty and `-Am` silently stops working, which for a station whose commit cadence is `-Am`-shaped means the trajectory stops being committed at all. Recommend a stage-0 guard test asserting `dolt status` is clean after a fold write, plus an explicit ban on `--force` in any grid tooling. (2) NEW, not in the draft: the projections are not branch-safe. The in-server DOLT_CHECKOUT case makes every proj_* table vanish, and §10.7's inherited \"never branch the primary checkout\" doctrine must be restated as a hard service invariant — the transition service must pin its session to one branch and fail closed if the branch changes, because a branch switch presents as a missing-table crash, not as data loss it can detect. (3) traj_pulse is explicitly non-rebuildable (§13 caveat) and is also swept away by the trap-recovery procedure; §10.3's \"restore + mandatory rebuild\" story should name the branch-switch and trap-recovery paths as additional ways liveness goes to `unknown`."
    },
    {
      "name": "T4 selective staging (decoupled commit cadence)",
      "question": "With dirty rows in both `trajectory` and a tracked non-grid table, does CALL DOLT_ADD('trajectory') + DOLT_COMMIT commit only trajectory and leave the other table's dirty state unstaged and intact?",
      "method": "Created and committed a tracked decoy table `decoy_bd` (stands in for a bd table on the shared db). Dirtied both: one INSERT into trajectory, one INSERT into decoy_bd. `dolt status` to confirm both modified. Then `CALL DOLT_ADD('trajectory'); CALL DOLT_COMMIT('-m','traj: seq 2..2 epoch 1');`. Re-ran status, dolt_diff_summary('HEAD~1','HEAD'), and compared `SELECT * FROM decoy_bd` (working) against `... AS OF 'HEAD'`.",
      "result": "Exact. Commit contained only `trajectory` (diff_summary: one row, from/to trajectory, modified). Status after: \"Changes not staged for commit: modified: decoy_bd\" — decoy still dirty. decoy_bd working = 2 rows including 'DIRTY-must-survive'; decoy_bd AS OF HEAD = 1 row. The dirty decoy then survived the entire remainder of the session — ~8,000 further trajectory appends and several more selective DOLT_COMMITs — still unstaged at the end. Side observation: diff_summary reports schema_change=true for a rows-only trajectory commit, because the AUTO_INCREMENT counter is table metadata.",
      "verdict": "pass",
      "implications": "§5's dolt-commit policy (\"staging only trajectory, traj_epoch, traj_terminal_guard\") is mechanically sound and durable under load — the service can hold its own commit cadence indefinitely without ever capturing bd's in-flight writes, and without bd's dirtiness blocking a trajectory commit. This is the strongest same-db result in the probe and materially weakens §10.2's flip-to-a-separate-database argument on the (b) cadence axis. Costs (a) attribution and (c) the force-trap remain, but (b) is now measured as a non-issue for the service's own commits — what the service cannot control is bd running `DOLT_COMMIT -a`, which would sweep trajectory rows under a `bd: update` message. Recommend §10.2 be rewritten to state that the flip condition is specifically \"bd (or any other writer) uses -a/-Am on the shared db\", which is checkable, rather than the general cadence concern."
    },
    {
      "name": "T5 AUTO_INCREMENT across restart and delete",
      "question": "Does `seq` continue monotonically across a sql-server stop/start with no reuse, including after deleting the max row (the InnoDB-pre-8.0 reset hazard)?",
      "method": "Through 127.0.0.1:13309: inserted 3 rows (seq 3,4,5), read information_schema.tables.AUTO_INCREMENT. Killed the server, restarted from the same db dir, re-read AUTO_INCREMENT and inserted. Then DELETE the max row (seq 6) and insert without restarting. Then DELETE the new max and restart the server before inserting.",
      "result": "No reuse in any case. Before restart AUTO_INCREMENT=6; after restart AUTO_INCREMENT=6 and the next insert got seq 6 (persisted across process death). Delete-max without restart: counter stayed at 7, next insert got seq 7 (gap at 6, no reuse). Delete-max WITH restart: counter was 8, next insert got seq 8 (gaps at 6 and 7, no reuse). Final table has seq 1,2,3,4,5,8 — monotone, gapped, never reused.",
      "verdict": "pass",
      "implications": "§1's \"seq = physical order, sole appender, one monotonic sequence\" holds across the crash/restart path, and dolt does NOT exhibit the InnoDB-pre-8.0 counter-reset that would have made seq reuse possible after a delete-then-restart. The §10.7 belt (uq_epoch_seq + the seq/epoch_seq agreement alarm) does not need to also guard against restart-induced seq reuse. Note for §1: seq is monotone but NOT gapless — a failed/rolled-back append burns a seq. Any fold or consumer that reads gaps as \"missing record\" would raise false corruption alarms; the draft should say explicitly that gaps are expected and only (boot_epoch, epoch_seq) is contiguous."
    },
    {
      "name": "T6 fence enforceability (a) invisible-read fence",
      "question": "§5's measured item: on dolt sql-server, does a stale appender that only READS MAX(traj_epoch.epoch) and writes elsewhere get refused when another connection advances the epoch mid-transaction?",
      "method": "t6.py. Conn A (autocommit off): BEGIN; SELECT MAX(epoch) FROM traj_epoch (reads fence=1, writes nothing to traj_epoch). Conn B (separate connection, autocommit on): INSERT INTO traj_epoch VALUES (2,...,'steal',...) — committed while A is open. Then A inserts a trajectory row stamped boot_epoch=1 and COMMITs.",
      "result": "A's COMMIT SUCCEEDED. The stale row landed: seq=9, boot_epoch=1, alongside epochs 1 and 2 in traj_epoch. B's insert took 5 ms and was never blocked.",
      "verdict": "fail",
      "implications": "The draft's feared outcome is CONFIRMED, verbatim: dolt's optimistic concurrency detects conflicts only on rows a transaction WROTE, and a read-only fence check is invisible. §5's \"⚠ Fence enforceability is a MEASURED tg-zfek item\" is now measured and the answer is no — the sentence should be rewritten from a caveat into a finding, and §8 Q15 / §11 item 15 must stop describing the epoch row as supplying \"the fence\" and describe it as supplying detection only, unless the T6i construction below is adopted. The uq_epoch_seq fallback (see T6f) is therefore load-bearing today, not a contingency."
    },
    {
      "name": "T6 fence enforceability (b) SELECT ... FOR UPDATE",
      "question": "The §12 dissent's CAS-upgrade question: does dolt honor MySQL row-lock semantics — blocking B, conflicting at commit, or ignoring the lock?",
      "method": "Two variants. (b1, t6.py) A: BEGIN; SELECT MAX(epoch) FROM traj_epoch FOR UPDATE; B concurrently INSERTs the next epoch; A appends and commits. (b2, t6ext.py) row-targeting rather than aggregate — A: SELECT epoch FROM traj_epoch WHERE epoch=1 FOR UPDATE; B then both INSERTs epoch 2 AND runs UPDATE traj_epoch SET pid=999 WHERE epoch=1 (the very row A locked); B's work timed, with a 10 s block detector; then A appends and commits.",
      "result": "IGNORED, in both variants. b1: B's insert completed in 4 ms, A's commit SUCCEEDED, stale row seq=10 landed. b2: B's INSERT ok AND B's UPDATE of the exact locked row ok, total 10 ms, blocked_10s=False; A's commit SUCCEEDED and the stale row landed. FOR UPDATE neither blocked a writer nor produced a conflict at commit.",
      "verdict": "fail",
      "implications": "The §12 dissent entry 'Fence primitive: ... vs fence-row FOR UPDATE CAS' must be closed as NOT AVAILABLE. Its stated condition — \"if stage 0's measurement proves dolt honors it, the CAS is a strict upgrade worth adopting\" — is falsified: dolt 2.2.2 parses FOR UPDATE and does nothing with it, which is the worst possible shape (silently no-op rather than erroring), so any code written against it would read as correct and fence nothing. Recommend the draft state this explicitly so the idea is not re-proposed."
    },
    {
      "name": "T6 fence enforceability (c) concurrent epoch claim race",
      "question": "Two connections both INSERT epoch MAX+1 concurrently — who wins and what does the loser see: duplicate PK error, or serialization error?",
      "method": "t6.py t6c(). Read MAX(epoch)+1 = 3. Two threads, each its own connection with autocommit off: BEGIN, threading.Barrier(2) rendezvous, INSERT INTO traj_epoch VALUES (3,...), sleep 0.25 s to widen the window, COMMIT.",
      "result": "Exactly one winner. X: 'WON (commit ok)'. Y: OperationalError (1213, 'serialization failure: this transaction conflicts with a committed transaction from another client, try restarting transaction.'). Final traj_epoch: epochs 1, 2, 3 with pid 901 (X). The loser sees a SERIALIZATION failure, NOT a duplicate-PK error — dolt's transaction conflict detection fires before the PK constraint can arbitrate.",
      "verdict": "pass",
      "implications": "§5's epoch-claim design (\"a single atomic INSERT into traj_epoch; the PK serializes competing claimers\") works, but for a different reason than §4's comment states. The comment '-- the atomic claim: a single INSERT; PK serializes competing claimers' should be corrected: the PRIMARY KEY is not what arbitrates — dolt's optimistic write-write detection is, and it surfaces as MySQL error 1213, a retryable serialization failure. The service's epoch-claim path therefore needs a 1213 handler (re-read MAX(epoch), decide whether to retry or refuse) and must NOT be written to catch 1062 duplicate-key. Note the two error classes are not interchangeable: T6f shows unique-key races surface as 1105 constraint-violation instead."
    },
    {
      "name": "T6d/T6g/T6h fence-row CAS shapes (write-touch)",
      "question": "If a read-only fence is invisible, can the appender make the fence check a WRITE so dolt's conflict detection sees it? Test a single-row fence table under three shapes: different column, same column same value, same column different value.",
      "method": "t6ext.py + inline. Built traj_fence(id=0, epoch, touch). (d1) A BEGINs and reads the fence; B commits UPDATE traj_fence SET epoch=2; A then runs the guarded UPDATE traj_fence SET touch=touch+1 WHERE id=0 AND epoch=1 and reports affected-rows. (d2) A write-touches `touch` (different column) while B writes `epoch` (same row); A commits. (g) A writes the SAME column with the SAME value: UPDATE ... SET epoch=epoch WHERE id=0 AND epoch=1, B sets epoch=2. (h) A writes the SAME column with a DIFFERENT value: A sets epoch=7, B sets epoch=2.",
      "result": "d1: A's guarded UPDATE matched 1 row even though B's steal to epoch=2 had already committed — the CAS predicate reads A's snapshot and is BLIND to the steal. A's commit then SUCCEEDED. d2: A's commit SUCCEEDED; final fence row = (id=0, epoch=2, touch=1) — dolt merged the two writes CELL-WISE, so a different-column write-touch fences nothing and leaves no trace of the conflict. g: SET epoch=epoch affected 0 rows (MySQL changed-rows semantics for a no-op write), no conflict, A's commit SUCCEEDED. h: A's commit REFUSED — OperationalError (1213, 'serialization failure ...'). So conflict detection is per-CELL and fires only when both transactions write the same cell to DIFFERENT values.",
      "verdict": "partial",
      "implications": "This is the mechanism the draft needs to know: dolt's conflict unit is the CELL, and a same-cell write is only a conflict if the values differ. Consequences for §5: (1) an in-transaction CAS predicate cannot be trusted — snapshot isolation makes WHERE epoch=<expected> match a stale value, so affected-rows is NOT a stale-fence signal; (2) a fence-row \"touch\" in a side column is silently cell-merged, which would have produced a fence that looked implemented and enforced nothing. Both are exactly the shape of latent bug the grid has shipped before. The one construction that does work is T6i."
    },
    {
      "name": "T6i counter-CAS — a fence that IS enforceable on dolt",
      "question": "Does a single-cell fence whose every appender-touch writes a guaranteed-different value actually refuse a stale appender, in both interleavings?",
      "method": "Built traj_fence2(id TINYINT PK, fence_state BIGINT), seeded fence_state = epoch<<20 (the draft's own token encoding). Appender A, inside its txn, runs UPDATE traj_fence2 SET fence_state = fence_state + 1 WHERE id=0 AND (fence_state>>20) = <expected_epoch> — an unconditional value change to the same cell the advancer writes. Epoch advancer B runs UPDATE traj_fence2 SET fence_state = <new_epoch><<20 WHERE id=0. Ran both interleavings: B advances BEFORE A's touch, and B advances AFTER A's touch, in each case with A committing last and appending a trajectory row under the stale epoch.",
      "result": "BOTH interleavings REFUSED. Each time A's guarded UPDATE reported matched 1 row (predicate blind, as expected) but A's COMMIT raised OperationalError (1213, serialization failure). Final check: `SELECT idem_key FROM trajectory WHERE idem_key LIKE 't6i%'` returned EMPTY — zero stale appends landed.",
      "verdict": "pass",
      "implications": "A dolt-enforceable fence EXISTS and should replace §5's detect-and-halt-only posture. Required shape, all three properties load-bearing: (i) ONE cell that both the appender and the epoch-advancer write; (ii) the appender's write must always change the value (a monotonic counter in the low bits — which the draft's `(epoch << 20) | counter` token encoding already provides for free); (iii) the appender must treat error 1213 at COMMIT, not the UPDATE's affected-row count, as the stale-fence signal. This turns §5 step 1 (\"assert boot_epoch == MAX(traj_epoch.epoch)\") from a no-op read into a real refusal, and makes the §12 dissent's \"strictly stronger\" fence available after all — not via FOR UPDATE, which is dead, but via same-cell write conflict. Costs to state: the fence cell becomes a global serialization point (any two concurrent appenders now conflict with each other on it — acceptable under the sole-appender premise, fatal if a second appender ever exists, which is the same condition §12 already names for revisiting the storage shape); and it adds ~7 ms per append (T7 E vs D). Recommend: adopt as the primary fence, KEEP uq_epoch_seq + the seq/epoch_seq alarm as the belt, and drop 'no design's fence is provably enforceable on dolt' from §1's epoch_seq note."
    },
    {
      "name": "T6e/T6f concurrent appenders — what the fallback actually catches",
      "question": "Do two appenders writing DIFFERENT trajectory rows conflict, and does the uq_epoch_seq fallback fire when they collide on the same (boot_epoch, epoch_seq)?",
      "method": "t6ext.py. (e) Two barrier-synchronized threads each BEGIN, INSERT a trajectory row with distinct epoch_seq (600, 601), sleep 0.25 s, COMMIT. (f) Same but both use epoch_seq=700.",
      "result": "e: BOTH committed — {'X': 'committed', 'Y': 'committed'}, seqs 14 and 15. Distinct rows in the same table do not conflict; the trajectory table itself provides zero mutual exclusion. f: one committed (Y, seq 16), the other REFUSED with error 1105: 'Committing this transaction resulted in a working set with constraint violations, transaction rolled back ... Type: Unique Key Constraint Violation, Name: uq_epoch_seq, Columns: [boot_epoch epoch_seq] (2 row(s))', plus dolt's hint about @@dolt_force_transaction_commit=1.",
      "verdict": "pass",
      "implications": "§5's fallback works but its scope is narrower than the draft implies. It catches only appenders that pick the SAME epoch_seq; two stale appenders that pick different epoch_seq values both land silently (T6e), and the seq/epoch_seq disagreement alarm in §1 is then the only remaining detector — post-hoc, not preventive. Three concrete notes for the draft: (1) the refusal is error 1105, a different class from the 1213 the epoch race produces — the service needs both handlers and must not conflate them; (2) dolt's own error text advertises @@dolt_force_transaction_commit=1 as the way to commit anyway — that session variable must be explicitly banned in the transition service's connection setup, since setting it would silently disable the entire uq_epoch_seq fallback; (3) because a split-brain appender allocates epoch_seq from its own stale view, same-value collision is likely but not guaranteed, which is the strongest argument for adopting the T6i fence rather than relying on the fallback."
    },
    {
      "name": "T7 per-append transaction throughput",
      "question": "§5's 8-attempt-storm feasibility number: what is the wall-clock and rows/sec for ~2,000 single-row INSERT-in-txn appends through sql-server, and what do DOLT_COMMIT cadence, synchronous projection writes, and the fence CAS each cost?",
      "method": "t7.py, N=2000 per run, one persistent connection, autocommit off, one BEGIN/INSERT/COMMIT per row, ~124-byte JSON payload, each run under its own boot_epoch. Variants: A bare append; B + CALL DOLT_ADD('trajectory') + DOLT_COMMIT every 512; C same every row; D append + one proj_step_cursor INSERT..ON DUPLICATE KEY UPDATE in the SAME txn; E D + the T6i fence CAS write in the same txn; F D+E+DOLT_COMMIT every 512. IMPORTANT: an initial pass reported per-row DOLT_COMMIT as free — that was an artifact of calling DOLT_COMMIT without staging, so --skip-empty made every call a no-op (dolt_log confirmed 4 commits, not 2000). Re-run with DOLT_ADD before each commit; all numbers below are from the corrected runs and were verified against COUNT(*) FROM dolt_log.",
      "result": "A sql-txn only, no dolt commit: 16.86 s, 118.6 rows/s, 8.43 ms/append. B + DOLT_COMMIT every 512 (3 real commits): 17.60 s, 113.6 rows/s, 8.80 ms/append — ~4% cost. C + DOLT_COMMIT every row (2000 real commits): 46.44 s, 43.1 rows/s, 23.22 ms/append — 2.6x slower. D append + ONE projection upsert in-txn: 31.91 s, 62.7 rows/s, 15.96 ms/append — the single projection write roughly DOUBLES the append. E D + fence CAS: 45.55 s, 43.9 rows/s, 22.77 ms/append — the fence adds ~6.8 ms. F full realistic (append + 1 projection + fence + commit/512): 47.36 s, 42.2 rows/s, 23.68 ms/append.",
      "verdict": "pass",
      "implications": "Headline for §5 and the tg-y4fd soak plan: a realistic fenced append with ONE synchronous projection costs ~24 ms and the sole appender sustains ~42 appends/s. That is comfortable for an 8-attempt storm at grid tempo but the margin is thinner than the draft assumes, because §6 requires the append transaction to update EVERY synchronous projection (P1-P6 and P8, not one) — D measures the marginal cost of the FIRST projection write at ~7.5 ms, so a transition touching P1+P2+P5 plausibly lands at 35-45 ms, i.e. ~22-28 appends/s. §5's pre-authorized retreat to a batched class should be scoped against that number, not against the 118 rows/s bare figure. Two findings that support the draft as written: the decoupled commit cadence is nearly free (4%), and per-row dolt commits cost 2.6x latency on top of the storage cost in T8 — the 30 s / 512-row policy is correct and well-margined. One finding against a draft assumption: §12's dissent frames per-append transactions as the throughput risk, but the measured bottleneck is not the transaction — it is the number of WRITES inside it, so group-commit batching would help less than shrinking the synchronous projection set would. Also note the measurement trap for the soak harness: DOLT_COMMIT without a preceding DOLT_ADD is a silent no-op under --skip-empty, so any soak that measures commit cadence must assert COUNT(*) FROM dolt_log rather than trusting the call."
    },
    {
      "name": "T7b commit-cadence storage cost (isolated)",
      "question": "Quantify the 544,956-commit / 18 GB pathology the draft's dolt-commit policy exists to prevent: what does per-row DOLT_COMMIT cost in permanent storage versus every-512?",
      "method": "Two FRESH databases (cad1, cad512), identical trajectory DDL, 2000 identical appends each (~90-byte payload), the only difference being DOLT_ADD+DOLT_COMMIT every 1 row vs every 512. Measured `du -sk .dolt` raw, then ran `dolt gc` and measured again, plus COUNT(*) FROM dolt_log.",
      "result": "cad1: 2001 dolt commits, 41.82 s, raw 56,492 KB, AFTER GC 29,248 KB. cad512: 4 dolt commits, 16.96 s, raw 46,160 KB, AFTER GC 604 KB. That is 48x permanent storage for identical data, and gc cannot reclaim it because the commits are reachable history — ~14.6 KB of unreclaimable history per dolt commit.",
      "verdict": "pass",
      "implications": "§5's dolt-commit policy is now backed by a hard number rather than the historical anecdote: per-row commits cost 48x storage and 2.5x latency, and gc reclaims none of it. Worth stating in §10.4 (monotonic growth) since it reframes that section — the log's growth is dominated by COMMIT COUNT, not row count, so the boundary-event triggers in §5 (epoch advance, attempt.terminal, attempt.round.retired) are the thing to watch: at 8 attempts with frequent terminals those could push the cadence far above the stated ~2,880/day floor. Recommend the policy add a hard minimum interval between dolt commits regardless of boundary events. This also refines the operator's standing note that 'gc reclaims nothing' — true for committed history (confirmed here), but gc DOES reclaim the uncommitted transaction journal, which T8 shows is large."
    },
    {
      "name": "T8 JSON payload behavior and storage amplification",
      "question": "How does dolt diff render JSON columns (is §10.6's 'dolt diffs JSON structurally' true?), and does large JSON bloat the commit graph disproportionately versus many small payloads?",
      "method": "Three isolated fresh dbs, trajectory table only, DOLT_COMMIT every ~1/4 of the run so commit counts match (5 each). jsmall: 2000 rows x 124-byte payload (242 KB total). jlarge: 200 rows x 3902-byte payload of repeated 'x' (762 KB total). jrand: 200 rows x 3924-byte INCOMPRESSIBLE payload (base64 of os.urandom(2900), 766 KB total) — added as a control after jlarge's compressibility was noticed. Measured `du -sk .dolt` raw and after `dolt gc`. Separately: UPDATE trajectory SET payload=JSON_SET(payload,'$.state','complete') WHERE seq=1 in jsmall, then CLI `dolt diff`, `dolt diff --stat`, and SQL `SELECT from_payload, to_payload, diff_type FROM dolt_diff('HEAD','WORKING','trajectory') -r json`; plus `dolt diff HEAD~1 HEAD` and `--stat` over a large-JSON INSERT commit in jrand.",
      "result": "SIZE: jsmall 242 KB payload -> raw 48,208 KB, after-gc 544 KB (2.25x amplification vs payload). jlarge 762 KB payload -> raw 4,304 KB, after-gc 100 KB (0.13x; compressible artifact). jrand 766 KB INCOMPRESSIBLE payload -> raw 4,308 KB, after-gc 884 KB (1.15x). So large JSON does NOT bloat disproportionately — it is MORE efficient per byte; per-ROW overhead dominates for small payloads. Pre-gc journal is ~22-24 KB per appended row REGARDLESS of payload size (48 MB per 2000 appends), essentially all reclaimable by gc. DIFF: §10.6's claim is FALSE as stated for the CLI. `dolt diff` on a single changed JSON key renders the ENTIRE row twice — all 37 columns, with the WHOLE payload value on each side — as a `<` / `>` table-row pair; there is no key-level JSON diff. `dolt diff --stat` gives row/cell counts only ('1 Row Modified (0.05%)'; for the insert commit '47 Rows Added, 1,692 Cells Added'). The SQL `dolt_diff()` function returns from_payload/to_payload as parsed JSON objects — still whole-value, but programmatically addressable so a caller can JSON_EXTRACT and compare keys itself. Storage accounting IS cell-level; only the rendering is whole-row.",
      "verdict": "partial",
      "implications": "§10.6 needs two corrections. (1) 'dolt diffs JSON structurally' is wrong at the operator surface — replace with: dolt stores and accounts JSON per cell, but renders it whole-value across the full row, so a 16 KB rationale prints ~32 KB inside one wide table row and the `traj show` verb (§4) must do its own typed payload formatting rather than delegating to dolt diff. The ≤ 16 KB rationale bound and the digest-reference rule for diffs are still right, now for a rendering/legibility reason rather than a storage reason. (2) The size premise is inverted: the draft worries about large payloads, but the measurement says per-append overhead dominates and few-large beats many-small (1.15x vs 2.25x amplification). The real storage lever is append COUNT and commit COUNT (T7b), not payload size — which strengthens the §2 decisions to coalesce heartbeats out of the log and to make molecule.poured one record replacing a ~6k-bead pour. NEW operational finding for §10: the uncommitted transaction journal grows ~22-24 KB per append, i.e. roughly 48 MB per 2000 appends, before any dolt commit. At the measured 42 appends/s storm tempo that is on the order of 1 MB/s of journal on the shared bd database. gc reclaims ~99% of it, but §10.1's interaction contract must now say that periodic gc is MANDATORY, not hygienic — and gc on the shared store is exactly the operation the standing dolt-server-kill/proxy-cleanup hazard applies to, so the stage-0 test case named in §10.1 ('first dolt gc / bounce taking the service down ungracefully') is more load-bearing than the draft treats it."
    }
  ]
}
