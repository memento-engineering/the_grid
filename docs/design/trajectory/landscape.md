# Landscape memo — agent trajectories, (A) observability vs (B) durable ledger

**For:** the architect of the grid trajectory store (`the_grid/docs/design/trajectory/trajectory-schema.md`, V2, tg-hnlt)
**Date:** 2026-08-31 · **Sources:** three sweeps (standards bodies · Dart ecosystem · harness/eval formats)
**Reference contract (B):** typed append-only observer-written records · single fenced appender · explicit-unknown outcomes · verification bound to immutable digests · backfill/provenance markers · rebuildable fold.

---

## 1. THE MAP

### Standards & conventions — all category (A)

**OpenTelemetry GenAI semantic conventions (`gen_ai.*`) — (A).** The industry's converging span vocabulary: `invoke_agent` spans containing `chat` and `execute_tool` children, ops for `create_agent`/`invoke_workflow`/`plan`/retrieval/memory. As of the 2026-06-12 v1.42.0 release, **every** `gen_ai.*` attribute, span, metric, and event is stability level "Development" — none Stable, no public stabilization timeline — and the whole namespace was just spun into its own repo (`open-telemetry/semantic-conventions-genai`) for a faster cadence, which is a cadence move, not a graduation. The spec discusses sampling and permits instrumentations to truncate message content: lossy by design, by its own text. Registry check is the load-bearing negative: there is `gen_ai.conversation.id`, `gen_ai.agent.id/name/version`, and a `gen_ai.response.status` lifecycle enum — and **no idempotency key, no sequence number, no digest/verification attribute anywhere**. → https://github.com/open-telemetry/semantic-conventions-genai/blob/main/docs/gen-ai/gen-ai-agent-spans.md

**OpenInference (Arize) and OpenLLMetry (Traceloop) — (A), and converging into OTel.** OpenInference is "built on OpenTelemetry… every OpenInference trace is a valid OTLP trace"; its contribution is a span-kind taxonomy (LLM/CHAIN/TOOL/RETRIEVER/AGENT/GUARDRAIL/EVALUATOR). OpenLLMetry's README now states flatly "Our semantic conventions are now part of OpenTelemetry" — it folded rather than competed (Traceloop acquired by ServiceNow, March 2026). Net: the span-vocabulary landscape is consolidating to one namespace, and that namespace is (A). → https://arize-ai.github.io/openinference/spec/

**MCP and A2A — deliberately *not* in this business; strong negative evidence.** MCP's 2026-07-28 spec **removed protocol sessions entirely** ("we've officially retired the `initialize`/`initialized` exchange along with the `Mcp-Session-Id` header"); state is explicitly pushed to the application ("mint an explicit handle from a tool and have the model pass it back"), and long-running Tasks moved to an optional poll-based extension with no history concept. A2A (Linux Foundation, v1.0) *has* a `Task.History` field and then disclaims it in spec text: "not all Messages are guaranteed to be persisted in the Task history"; "Messages MUST NOT be considered a reliable delivery mechanism for critical information"; `SendMessage` is only "MAY be idempotent." An execution-history field that the spec goes out of its way to make unreliable is the cleanest anti-(B) artifact in the survey. → https://blog.modelcontextprotocol.io/posts/2026-07-28/ · https://a2a-protocol.org/latest/specification/

**CNCF / W3C / LF consortium work — no (B) work item exists.** CNCF's March 2026 agentic-standards position piece recommends "trajectory based assessment" but operationalizes it as OTel + canonical logging + generic "tamper-proof logging systems"; the named standards it surveys (MCP, A2A, AP2, SPIFFE/SPIRE, AGNTCY Identity, OWASP ANS, Gateway API Inference Extensions) are tools/identity/payments/discovery — none is an execution record. W3C's only relevant artifacts are the generic PROV-O ontology and a new **AI Agent Memory Interoperability CG** (charter adopted 2026-06-19) — memory interop, not execution ledgers. No W3C/IETF/CNCF working group targets durable agent execution records. → https://www.cncf.io/blog/2026/03/23/cloud-native-agentic-standards/

### Harness / eval trajectory formats — (A)-shaped serialization, one near-(B)

**ATIF — Agent Trajectory Interchange Format (Harbor, v1.8, April 2026) — (A)/serialization.** The most real cross-harness effort: root `{schema_version, session_id, trajectory_id, agent, steps[], notes, final_metrics, continued_trajectory_ref, subagent_trajectories, extra}`; steps carry `step_id, timestamp, source, model_name, message, reasoning_content, tool_calls, observation, metrics, llm_call_count, is_copied_context`. Genuine adoption: Harbor converts OpenHands event logs to ATIF, supports Mini-SWE-Agent, Claude Code, Gemini CLI, and any agent setting `SUPPORTS_ATIF = True`; terminal-bench 2.0 runs on Harbor. `is_copied_context` (with a "MUST NOT be included in SFT data" rule) is provenance-adjacent — but scoped to *training-data hygiene*, not correctness. **No idempotency, no fencing, no digest verification documented.** → https://github.com/harbor-framework/harbor/blob/main/rfcs/0001-trajectory-format.md

**Letta "Trajectory" (`@letta-ai/trajectory`, `trajectory-v1.schema.json`) — (A)/serialization, narrower.** Five record types (meta/user/reasoning/assistant/tool), explicitly optimized for *agent-consumable token efficiency* (~5.6× reduction), explicitly **dropping** harness bookkeeping and truncating long tool results. Single-vendor, no third-party adoption found. It is the only thing on the market claiming the *word* "trajectory" as a format name — worth knowing for naming collisions, worthless as a correctness substrate. → https://www.letta.com/blog/trajectory/

**SWE-agent `.traj` files — (A), de facto.** JSON per instance, per-step `{response, thought, action, observation, state, query}`, plus config for exact re-run; consumed by a viewer and `traj-to-demo`. No append-only/write-once statement. The oldest de facto format; ATIF is the attempt to generalize it. → https://github.com/SWE-agent/SWE-agent/blob/main/docs/usage/trajectories.md

**OpenHands `EventLog` — the closest (B)-*mechanics* in the harness world.** Stateless `Agent` emitting Actions; `Conversation` maintains a genuinely **append-only EventLog**, `ConversationState` described as "single source of truth," persisted as `state.json` plus one JSON file per event for incremental writes; the V1 SDK redesign leans on it explicitly. But: local file per conversation, single-process, no fencing/multi-writer story, no idempotency keys, no digest-bound verification, no explicit-unknown outcome. Append-only as an *implementation convenience*, not as a correctness contract. → https://github.com/OpenHands/OpenHands/issues/6049

**UK AISI Inspect `.eval` logs — (A) with a real audit axis.** Binary `.eval` (⅛ the size of JSON, incremental sample access) or `.json`, same `EvalLog` API; records samples, transcripts, events, statistics — and distinctively, **provenance for post-hoc edits**: a score modification appends a `ScoreEditEvent` rather than overwriting. That's write-once-then-amend with an audit trail, which structurally rhymes with our `resolves_record_id` settlement link. No cryptographic/digest verification; "authoritative" is metadata-tracked, not digest-bound. → https://inspect.aisi.org.uk/eval-logs.html

**Commercial tracing platforms (Langfuse, LangSmith, Braintrust, W&B Weave, AgentOps) — unambiguously (A).** All five are span/trace trees for debugging and analytics, all converging on OTLP ingestion. Diagnostic details worth carrying: Langfuse uses "authoritative" in its docs only about *which ingest path sets `start_time`/`end_time`* — a timestamp field's provenance, not the record's evidentiary status; LangSmith's bulk export (Parquet→S3) went **Enterprise-only for new signups on 2026-08-03**, i.e. even getting your own data out is a commercial tier, not a guarantee; W&B Weave's 2026 redesign made `sessions/turns/steps/tools/sub-agents` first-class, the strongest move toward agent-native structure; AgentOps ships "time-travel debugging" from session-state **snapshots**, the closest commercial thing to replay and still framed as visual debugging. → https://langfuse.com/docs/observability/data-model · https://docs.langchain.com/langsmith/data-export

### Durable-execution frameworks — where real (B) mechanics live

**Temporal — (B) mechanics, general-purpose.** The strongest structural match found anywhere. "The authoritative state of every workflow is the sequence of events it emitted, not [any derived] blob." Real fencing: `db_record_version` is a literal optimistic-concurrency fencing token per workflow, plus a monotonic per-shard `RangeID` — single-writer semantics at two granularities. Effectively-once Activities; non-deterministic results are *recorded into history* so replay reuses them (fold-from-log). `Query` reads state without touching history. Gaps vs. our contract: no explicit-unknown vocabulary, no digest-bound verification step, no agent-native typing (tool calls/model turns arrive via wrapper libs — the OpenAI Agents SDK integration hit GA 2026-03-23), 2MB default payload cap. → https://github.com/temporalio/temporal/blob/main/docs/architecture/history-service.md

**DBOS — (B) mechanics, and the closest *philosophical* ally.** No separate history service: workflow and step state live in **your own Postgres** under a `dbos` schema (`dbos.workflow_status`, `dbos.operation_outputs`, one row per step execution), queryable with plain `psql`. Ships a `DBOSDurability` capability routing agent model/MCP calls through DBOS steps. This is "the ledger is just a database you already own" — the same instinct as a grid-owned dolt store, minus versioning. → https://docs.dbos.dev/explanations/system-tables

**Restate and Inngest — partial (B).** Restate journals each `ctx.run()` step *before* execution and skips completed steps on replay, explicitly pitched at "multi-tool agent loops"; no fencing-token/single-writer language comparable to Temporal's. Inngest does step **memoization** (not deterministic replay), and in June 2026 added "Insights" (SQL over full execution data, no export) and a CLI `api` subcommand so a coding agent can pull step traces from CI — the most "queryable ledger for agents" positioning of the four, still framed as ops tooling. → https://restate.dev/what-is-durable-execution · https://www.inngest.com/blog/durable-execution-key-to-harnessing-ai-agents

**The cleanest external statement of our own (A)/(B) distinction**, useful for any doc or README that has to explain why this exists: *"A trace can explain what happened; a durable journal must decide what may be replayed… observability is not the same thing as recovery"* — and, separately, "the biggest trap is believing that checkpoints alone solve durability," with Stripe-style tool-call idempotency called out as a *distinct* missing concern from checkpointing. → https://zylos.ai/research/2026-04-24-durable-execution-agent-runtimes/

### Dart ecosystem — effectively empty

**Genkit Dart — the only real (A) in Dart.** Preview March 2026, 0.14.0 May 2026; inherits the shared Genkit "action" tracing primitive: automatic traces of every flow, tool call, model invocation, and streamed chunk, tree-viewed in the local Developer UI at `localhost:4000`, exported in production via the Firebase plugin to Google Cloud Logging/Trace/Monitoring. Dart rides the same primitive as JS/Go/Python with no Dart-specific store. Architecturally ordinary: dev inspection + cloud telemetry, no fencing/idempotency/system-of-record claim. → https://genkit.dev/docs/local-observability/

**Everything else in Dart is weaker than (A).** `langchain_dart` exposes `AgentExecutor.returnIntermediateSteps` — an **in-memory return value only**; the entire Python callback/`LangChainTracer` subsystem was never ported. `dartantic_ai`'s README has zero occurrences of tracing/telemetry/observability. `flutter_ai_toolkit` persists chat memory, not execution. `agents` (a port of Microsoft's agent-framework) has the best *seam* — `AgentSession`, a pluggable `ChatHistoryProvider`, middleware slots named for "logging, telemetry, evaluation" — but ships no backend. `dart_agent_core` has the most interesting adjacent feature (Anthropic-aligned eval subsystem with record/replay and Langfuse export) over **snapshot** `FileStateStorage`; grepping its README for trajectory/ledger/append-only/idempotent/provenance/fenced/authoritative returns zero hits. `agentic_flutter`'s `EventRecorder` is bounded at 500 events, debug-only, non-persistent. → https://pub.dev/packages/agents · https://pub.dev/packages/dart_agent_core

**OpenTelemetry for Dart is pre-governance and has no GenAI conventions.** Two competing SDKs — Workiva's `opentelemetry` (v0.18.11, traces Beta / metrics Alpha) and `dartastic_opentelemetry` (v0.11.0, actively shipping, "in the process of being donated to the CNCF"). The formal OTel donation proposal (community#2718, opened May 2025) is **still open**, and OTel's own July 2026 "Call for Contributors: OpenTelemetry for Dart and Flutter" post confirms the SIG bootstrap proposal is under review. That post makes **zero** mention of GenAI/LLM/agent conventions — Dart OTel is scoped to foundational traces/metrics/logs. `otel_dio` exists but is transport-layer only: an LLM call becomes a generic HTTP span. → https://opentelemetry.io/blog/2026/dart-flutter-opentelemetry/ · https://github.com/open-telemetry/community/issues/2718

**pub.dev search is barren.** `?q=trajectory` returns ballistics, gesture recognition, animation, growth charts, a basketball game. `?q=ledger` returns 100% crypto/hardware wallets. `?q=agent+trace` returns UI inspectors and substring collisions. Adjacent event-sourcing: `eventuous` (v0.1.1, last published 3 years ago, "use it on your own risk"), `gossip_event_sourcing` (1 like, 31 downloads).

### Research literature — independent convergence on our shape, zero standardization

**"The Log is the Agent" / ActiveGraph (arXiv 2605.21997).** The one paper arguing our exact thesis: "the append-only event log is the source of truth; the working graph is a deterministic projection of that log," yielding deterministic replay, cheap forking, end-to-end lineage. Single-author research runtime; does not engage OTel/OpenInference at all. → https://arxiv.org/abs/2605.21997

**"Auditable Agents" (arXiv 2604.05485, ACM AI Leadership Summit 2026).** Frames the gap as tamper-evident records (signed, hash-chained; 8.3ms median overhead per intercepted tool call via their "Aegis" firewall) and — the genuinely useful export — defines **five auditability dimensions**: action recoverability, lifecycle coverage, policy checkability, responsibility attribution, evidence integrity. Their honest finding is that recovery is only *partial* even with the tooling. → https://arxiv.org/abs/2604.05485

**Also live, all prototypes:** PROV-AGENT (arXiv 2508.02866) extends W3C PROV for agent/tool/LLM interactions, author-described as awaiting community adoption; Graph of Trace (ACL 2026 demo) is an append-only JSON ledger for scientific agents whose append-only property is justified because it "prevents retrospective modifications which could introduce context noises"; LEDGER (arXiv 2608.18398) does claim→evidence trace graphs for audit; "From Agent Traces to Trust" (arXiv 2606.04990) surveys the space and — tellingly — treats (A)-observability and (B)-audit/recovery as *points on one taxonomy* rather than separating them. A curated tracker (`awesome-auditable-ai`) exists, which is itself evidence of fragmentation.

### The neighbor: beads on dolt

**Beads' Dolt migration is the most directly parallel real deployment — and it solves a different problem.** `bd init --backend dolt` is live (DoltHub, Jan 2026); the forcing function was **concurrency, not durability**: SQLite optimistic locking produced "unrecoverable chaos" past ~4 concurrent agents, and the git-style commit/branch/merge model is credited with taking Gas Town to ~160 agents/host and ~600 across a cluster. But what Dolt versions there is **mutable task/issue state with history-as-commits**, not an append-only execution record. Direct fetch of both DoltHub posts confirms they never use the words append-only, provenance marker, idempotency, fencing, or verification. No beads/Gas Town feature named "trajectory" or "execution ledger" was found. → https://www.dolthub.com/blog/2026-03-13-multi-agent-persistence/

---

## 2. THE GAP CLAIM, TESTED ADVERSARIALLY

**The claim as stated — "append-only fenced agent-trajectory ledger is unclaimed territory" — is false as written, and true after one narrowing.** Take the falsification seriously first:

- **Fenced append-only ledger with fold-from-log: occupied, and by a production-hardened incumbent.** Temporal is not adjacent prior art; it is the same mechanism. Event history as the authoritative state, `db_record_version` as a literal fencing token, `RangeID` for shard-level single-writer, effectively-once activities with results recorded into history so replay reuses rather than re-executes. Our `traj_fence` counter-CAS is a re-derivation of a pattern Temporal has run at scale for years. DBOS re-derives it again over plain Postgres tables you own. Restate journals-before-execute. If the claim is about *mechanism*, three companies got there first and one of them just raised a $300M Series D on the agent thesis.
- **Append-only agent execution record: occupied at small scale.** OpenHands' `EventLog` is genuinely append-only and genuinely described as single source of truth, per conversation.
- **Typed agent-trajectory record vocabulary: occupied.** ATIF v1.8 is real, cross-harness, and adopted (OpenHands, Mini-SWE-Agent, Claude Code, Gemini CLI, terminal-bench 2.0 via Harbor).
- **Provenance markers on execution records: occupied twice.** ATIF's `is_copied_context` and Inspect's `ScoreEditEvent` are both provenance-preserving amendment mechanisms in shipping tools.

**What survives.** No occupant holds the **intersection**, and the intersection is where the schema actually lives:

1. **Typed, agent-native record vocabulary** (attempt / admission / verification / effect / step families) — Temporal, Restate, DBOS, and Inngest have none; agent semantics are wrapper libraries bolted onto generic workflow durability, never native to the history schema.
2. **× a fenced single appender with idempotency keys mandatory on every record type** — ATIF, Letta Trajectory, SWE-agent, OpenHands, Inspect have none. Not one documents an idempotency key, and A2A explicitly disclaims idempotency for `SendMessage`.
3. **× explicit-unknown as a first-class, settleable outcome** — found **nowhere in any sweep**. Temporal has no unknown vocabulary. OTel has a `gen_ai.response.status` enum with `failed`/`cancelled` and no unknown. This is the single most differentiated element of the schema: `outcome='unknown'` + `unknown_reason` CHECK + a later record settling it via `resolves_record_id`, with the liveness detector required to emit `unknown` rather than `lost` when its pulse table is empty.
4. **× verification bound to immutable digests** — `verify.scope.pinned` (base/head/diff_digest), `sha_drift` stamped at record time, CI conclusions pinned to the `verify.scope.pinned` in force *at conclusion time*. The only comparable thing is a research prototype's hash-chaining (Aegis) — Inspect, ATIF, and all five commercial platforms have nothing here.
5. **× admission/settlement decisions reading the fold** — the true differentiator. Everyone else's execution record is *downstream* of decisions. Ours is *upstream*: `admission.refused{clause, snapshot_rev}` / `admission.restored` re-evaluated on the fenced service tick, grants issued against a versioned basis, refusal of `worktree-outstanding` from a projection. No source found positions any durable-execution framework as intentionally exposing admission read-paths this way.

**The strongest counterfactual, stated honestly:** "you should have built on Temporal." The rebuttal is real but it is a *situational* rebuttal, not an architectural one — Dart, a grid-owned dolt store, bd-coupled correlation keys (`work_bead_id`/`session_id`/`step_path`), no appetite for a hosted history service. DBOS is the ally that proves the situational rebuttal is legitimate: "it's just your own database's tables" is a shipping commercial position, not an eccentricity.

**One trap the literature makes easy to fall into, worth naming in the doc:** Temporal's event sourcing is *deterministic code replay* — history exists so that re-executing workflow code produces the same decisions. Ours is *observer-written testimony folded into projections* — nobody re-executes anything. Both get called "event sourcing," and conflating them will produce bad borrowing (e.g. someone proposing a 2MB payload cap, or determinism constraints on the fold, neither of which applies). The `provenance` enum (`observed`/`inferred`/`reconstructed`) is precisely the concept Temporal doesn't need and we do.

**Verdict:** genuinely unclaimed, but claim it precisely. Not "we built an append-only fenced ledger" (Temporal, 2015). The defensible sentence is: *a typed agent-execution ledger whose fenced append discipline and explicit-unknown settlement make it safe for admission and settlement decisions to read from — over a store the operator owns.* Nobody occupies that. And the honest caveat: three independent 2026 research groups converged on the same shape (ActiveGraph, Auditable Agents, Graph of Trace) without knowing about each other or about the observability world, which means the territory is unclaimed *and* about to be contested.

---

## 3. INTEROP RECOMMENDATIONS — cheap alignment worth doing NOW

Grep confirms the schema currently mentions OTel, gen_ai, and export **zero times** (the only `export` hits are `bd export` being killed and `attempt_id` exported into process env). Everything below is additive and read-side. **Hard invariant for all of it: nothing on this list may touch the append path.** The fence, the per-append transaction, and the `idem_key` uniqueness are the value; an exporter that can fail an append destroys the thing it was meant to make legible.

| # | Recommendation | Effort | What it buys |
|---|---|---|---|
| **I1** | **Align `verify.usage.telemetry` payload keys to `gen_ai.*` names.** Today: `model, tokens_in/out, cost_usd, premium_requests, num_turns, duration_ms`. Map to `gen_ai.request.model`, `gen_ai.usage.input_tokens`, `gen_ai.usage.output_tokens`, keep `cost_usd`/`premium_requests`/`num_turns` as grid-local extras. | **Hours.** One codec, one `type_version` bump — §2.6 already governs exactly this. Do it before the type has history worth migrating. | The single highest ratio in the memo. Any future dashboard, any OTel-fluent reader, any LLM asked to interpret the table already knows these names. Costs nothing, and it is *only* a naming convention — no dependency, no library, no stability risk from OTel's "Development" status. |
| **I2** | **Adopt three more `gen_ai.*` names where they land for free:** `gen_ai.conversation.id` ≈ `session_id`, `gen_ai.agent.name` ≈ the `rig` on `attempt.session.started`, `gen_ai.operation.name` as a derived label on the export side. Document the mapping in a short §-appendix table; do **not** rename envelope columns. | **Hours** (a doc table, not a schema change). | Makes the exporter (I3) mechanical instead of a design exercise, and makes the "how does this relate to OTel?" question answerable in one page instead of a meeting. |
| **I3** | **Design (don't build) a read-side OTLP exporter seam.** A projection reader that emits spans: `attempt.session.started`→terminal as an `invoke_agent` span; `step.transition` running→complete as child spans; `effect.intent`→`effect.ack` as `execute_tool`-shaped spans with the receipt as an attribute. Strictly a consumer of committed rows, cursored by `seq`, restartable, never in the append transaction. Ship the *seam definition* now; ship the exporter when someone actually wants a trace UI. | **Design: a day.** Implementation deliberately deferred — and note Dart has **no OTel SDK under OTel governance** (community#2718 still open) and **no `gen_ai` conventions at all**, so building the exporter today means picking between two pre-1.0 SDKs. | Locks in the architectural rule (export is downstream, always) while the cost of stating it is zero, and prevents the far more expensive mistake of someone later wiring telemetry into the appender because "we needed the span to be reliable." |
| **I4** | **Define an ATIF v1.8 export shape for a session.** The mapping is unusually clean: `session_id`→`session_id`; `(work_bead_id, round)`→`trajectory_id`; the rig→`agent`; ordered `step.transition` + `attempt.note` + effect records→`steps[]` with `step_id` from `record_id`; `verify.route.verdict` grades→`final_metrics`; **`continued_trajectory_ref`→the prior round retired by `attempt.round.retired`** (their field for the exact thing our round ladder does); molecule child steps→`subagent_trajectories`; and `provenance != 'observed'`→`is_copied_context`-style flagging so reconstructed rows never contaminate an SFT corpus. | **1–2 days** for a projection-backed exporter, once I2 exists. | Buys entry to the only cross-harness format with real adoption (Harbor/terminal-bench 2.0/Claude Code/Gemini CLI ingest it), which means grid runs become gradeable by external eval tooling and comparable against other harnesses' runs — with **zero** concession in the ledger itself, because ATIF is downstream serialization. Also a free correctness audit: anything in ATIF you cannot fill from the fold is a fold gap. |
| **I5** | **Borrow vocabulary, in the doc, with citations.** (a) Use "durable execution" and "fencing token" as the framing terms — Temporal's `db_record_version` is the exact analogue of `traj_fence`'s counter-CAS, and naming it makes the design legible to anyone who has run a workflow engine. (b) Put the Zylos line in the README ("a trace can explain what happened; a durable journal must decide what may be replayed"). (c) Run the schema once against the "Auditable Agents" five dimensions — action recoverability, lifecycle coverage, policy checkability, responsibility attribution, evidence integrity — as an external rubric you didn't write. | **Half a day.** | Kills the "isn't this just tracing?" question permanently, and (c) is the cheapest available adversarial review: a taxonomy from outside the project, applied to a design that has already survived two internal audit rounds and a certification round. |
| **I6** | **Explicitly scope OUT, in writing.** MCP sessions (removed from the protocol 2026-07-28 — do not build against them), A2A `Task.History` (spec-disclaimed as unreliable; never treat a received history as evidence), and any dependency on OTel GenAI *stability* (100% "Development", no timeline — alignment by naming only, never by contract). | **An hour** (a §-note). | Prevents a future contributor from importing a non-guarantee into a system whose entire value is guarantees. This is the same failure mode as trusting `bd export`. |

**Explicitly not recommended now:** adopting Letta's Trajectory format (single-vendor, and it *deliberately drops* harness bookkeeping — precisely our payload); adding hash-chaining/signatures (research-stage, and `idem_key` + `diff_digest` + `commit_sha` already give digest-binding where it decides anything); any Dart OTel SDK dependency before the CNCF donation resolves.

---

## 4. IF THE STANDALONE TOOL EVER HAPPENS

**Adopt:**
- **Temporal's fencing vocabulary and its read-path discipline.** `db_record_version`/`RangeID` as the naming precedent, and `Query`-style reads that never perturb the ledger. You already have both mechanisms; adopting the names is how the tool gets understood in ten seconds by the people most likely to evaluate it.
- **DBOS's positioning, wholesale.** "The ledger is tables in a database you already own, queryable with plain SQL, no hosted history service" is a validated commercial stance and it is *already* what a dolt-backed store is — with versioning as a strict superset. This is the tool's market position, pre-argued by someone else.
- **ATIF as the export format, permanently.** Do not invent a serialization. ATIF has the adoption, the RFC, and the fields (`continued_trajectory_ref`, `subagent_trajectories`, `is_copied_context`) that already match our round ladder, molecule steps, and provenance enum.
- **Inspect's amendment discipline as validation.** A government eval framework independently arrived at append-an-edit-event-rather-than-overwrite. That's confirmation `resolves_record_id` is the right shape — and our version is strictly stronger (digest-bound, idempotency-keyed).
- **Beads' actual lesson, which is not the one it looks like.** The Dolt migration was forced by **concurrency**, not durability — SQLite last-write-wins chaos past ~4 agents. Our fenced single appender is the answer to that same pressure, arrived at from the other direction. Cite it as the deployment evidence that dolt-under-agents works at ~160 agents/host, and be explicit that beads versions mutable *state* while the tool appends immutable *execution* — two adjacent problems that will otherwise be conflated by every reader who knows beads.

**Bridge:**
- **OTLP export, one-way, read-side.** The tool should *emit* `gen_ai.*`-shaped spans into whatever observability stack the user already runs (Langfuse, Weave, and Braintrust all ingest OTLP). Never ingest OTel as evidence — sampled input cannot become a system of record, and Langfuse's own use of "authoritative" for a timestamp field is the cautionary example.
- **Harness adapters.** Harbor already converts OpenHands→ATIF; the inverse (harness→ledger) is the adoption path for anyone not on the grid. Each adapter must stamp `provenance='reconstructed'` with a basis — importing a `.traj` file yields testimony, not observation, and the schema already has exactly the right vocabulary to say so.
- **The durable-execution frameworks, as substrates, not competitors.** A Temporal/DBOS/Restate shop already has the fence and the log; what they lack is the typed agent vocabulary and the admission read-paths. The tool's most credible non-grid shape may be **a schema + fold + admission layer that sits on someone else's durable substrate** rather than a full engine — a much smaller thing to maintain, and it turns the strongest counterfactual from §2 into a distribution channel.

**Ignore:**
- **The commercial tracing platforms as competitors or as models.** Different category, and LangSmith's move of bulk export to Enterprise-only (2026-08-03) is the structural argument against depending on any of them for anything evidentiary.
- **MCP and A2A as transports for trajectory data.** MCP removed sessions; A2A disclaims history reliability in spec text. Neither is a place to put a record of truth.
- **Waiting for a standard.** No standards body has a work item, OTel GenAI is 100% Development with no stabilization timeline, and the only (B)-engaged material is 2026 arXiv preprints that don't cite each other or the observability world. There is nothing to wait for.
- **Letta's Trajectory format.** Optimized for the opposite goal (token efficiency via dropping bookkeeping), single-vendor. Its only relevance is the **name collision**: if the tool ships publicly as "Trajectory," it lands on an existing npm package and blog post. Pick a different name.

**Shape, in one line:** a small Dart package that owns a versioned, fenced, typed execution ledger plus its fold and its admission read-paths — exporting ATIF to the eval world and OTLP to the observability world, importing harness traces as `reconstructed` testimony, and refusing on principle to be anyone's tracer. The Dart ecosystem is empty (no package on pub.dev is within a mile — `?q=trajectory` returns ballistics and a basketball game), which is both the opportunity and the honest warning: an empty category in a small ecosystem is only a moat if someone shows up to cross it.
